## Tasks
| # | Task | Notes |
|---|------|-------|
| 1 | Refresh g-wiki for M46 (v2.4.0) **+ the 2.5 freeze/fork re-scope** — wiki still names M46→M41→M45→M42 and M29–M34 as the live roadmap, now contradicting the README freeze section | deferred 2026-07-23 · widened 2026-08-10 (doc-review) |
| 3 | **Audit Wave A — doc/enum one-liners (fix-now sweep):** g-specialize architect table 4 wrong filenames (`g-specialize:260,272,283,290`) · telemetry "18 agents" roster omits doc-reviewer (**both** sites: `g-telemetry:67` AND its report template `:137` — audit-1 #1) · README stale gate-bypass claim (`README.md:316`) · §-letter headings, **both legs of audit-2 B1**: `rules/g-rules/J-memory.md:1` heading says "I" AND `rules/g-rules/I-project-tracking.md:1` carries no letter at all · ADR-010:61 T1-detector back-stamp · ADR-009:33 stale follow-ups · `architecture.md:19` severity vocab (says BLOCKING/WARNING/PASS, implemented contract is Critical/Major/Minor) · rust-axum architect name≠filename · M28:32 fifth renumbering site · M1/M2 archive links | * all file:line refs in audit reports 1, 2, 4, 6 |
| 4 | **Audit Wave B — small code fixes:** `session-prompt-count` reader/writer key mismatch (g-plan reads unkeyed, hooks write `.$SESSION_ID` — plan-time §A7 guard never fires; audit-5 F-1) · check-commit missing-lib fallback = unbounded `cat` (audit-3 W1) + fail-open note (W2) · post-commit-cleanup trailing `rm -f` unguarded (W4) · bare `8000`ms bound → named `*_MS` constant (audit-7) · doctor check for `integration-tier` existence (audit-5 F-7) · g-afk training-mode block missing vs §B rule (audit-5 F-2) | * audit reports 3, 5, 7 |
| 5 | **Audit Wave C — test teeth (M48 riders):** `hooks/pre-commit` ZERO execution coverage — new suite for the authoritative gate (audit-7 H1) · un-inert `test-workflow-checkpoint.sh:365` §13 (probe-proven green with guard deleted) · mode-detection assertion (probe P5) · falsifiability evidence 8/18 → convention per M48 | * fold into M48 /g-plan |
| 6 | **M45 rider — reviewer record-write contract:** 12 reviewer agents instruct "write report to output_file" but frontmatter grants Read/Glob/Grep only (verified 2026-08-11); explains M45's two record-write stall incidents. M45's design ADR decides: scoped Write grants (architecture rule already permits record-scoped) vs HQ-writes-records | * evidence: audit report 1 + M45 roadmap entry |
| 7 | **Audit remainder (warning + note tier) — carried so the gitignored reports aren't the only record.** Not yet scheduled, not silently dropped: §I "three hook scripts" under-describes the two-sentinel gate (audit-1 #4) · `/g-doctor` Check 16 still carries a TYPED 7-hook list — an 8th hook would escape drift detection (audit-1 #5; same derive-don't-type class as the founding g-init bug) · `g-docs/audits/` + `g-docs/archive/` unregistered subpaths (one finding, audit-1 #6) · 8 state surfaces bypass `GF_CLAUDE_DIR` resolution, ADR-005 (audit-3 W3) · audit-4 W3/W4 (profile spot-check leftovers) · audit-5 F-3–F-6 (lesser dead-pointer/state items) · audit-7 F4 + H2–H5 + H9 (fixture drift + coverage holes below pre-commit) · ADR-005 keying follow-up back-stamp (audit-6 N1) · fork-bound milestone files dual-source note (audit-6 N6) | triage at next /g-plan touching each area; full text in audit reports |

## Details

*(Task 2 — the whole-system audit — executed 2026-08-11 and archived to `g-docs/todo-done.md` per §I. Its remediation is tasks 3–7 above. Task numbers 3–7 are stable references from the 2026-08-11 handoff; the #2 gap is deliberate. Notes-column `*` on rows 3–6 marks an external pointer (row 7 points externally too but is unmarked — same settlement) to the named audit reports — this ledger currently carries no `### N` Details subsections; §I's asterisk convention is stretched knowingly until the next `/g-docs` settles it.)*
