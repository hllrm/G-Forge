# Alignment check — 2026-08-17 — M47 close swarm

**Verdict:** ALIGNED
**Brief dated:** 2026-07-01 (retroactive — project was live at v2.2.0 when written)
**Version:** 2.4.0
**Previous check:** 2026-07-23 (`.claude/last-align`) — 25 days, against a 7-day nudge cadence

## Goal service

- [✓] **Enforce the process the way a good team lead would** (hard commit gate) — actively served and the arc's spine. M47 shipped 2026-08-12 (`6590b60`, 4-round review); M48 Review-Pipeline Hardening is next in build order. Live defect logged rather than hidden: todo row 10, the commit gate misclassifying a doc-only heredoc commit as MIXED (reproduced 2026-08-16, fail-safe direction).
- [✓] **Control context and hallucination** — the strongest-served goal this pass. `/g-resume` Step 0 shipped 2026-08-16 (`8d53a4e`) after six review rounds; it is precisely the re-hydration half of the §A7 reset the brief names. `pre-compact` snapshotting and the compaction counter are live.
- [⚠→resolved same day] **Make the LLM govern itself honestly — "quality measured, not asserted"** — served in *plan* far better than in *fact* until today. The 2026-08-17 `/g-telemetry` run found five structural defects in the instruments this goal rests on: the metrics spec has no ship vehicle at all (no skill creates `g-docs/telemetry-metrics.md`, so `/g-telemetry` reads a missing file on every consumer project); the hallucination gauge matches zero bullets across 22 retros while four confabulations are on record; coverage scored `code-reviewer` as `never` on a day the journal records it running (`g-forge:code-reviewer start a8e145c2`, 2026-08-16); the rework metric cannot return to `stable`; and the agent-count consumer is a typed 18 against 19 on disk. **Not carried as drift** because the correction landed the same session — M50 Eval-Chain Integrity folded into 2.5 (ADR-012 amended 2026-08-17), sequenced second. Recorded here because the *detection* was manual and late, which is the actual exposure.
- [✓] **Stay opinionated but degradable** — three tiers live and enforced in the hook layer (`workflow-checkpoint.sh:170` exits on `light`). No new work needed; no neglect.
- [✓] **Scale governance from one session to many** — deferred by **recorded decision**, not neglect: the multiplayer arc (M29/M33/M34, M30–M32) is fork-bound per ADR-012 and lives in the local-only `g-proof-roadmap.md`. A recorded override is not drift.

## Scope creep

- [✓ none] Everything built this pass traces to a Goal. M50 was triaged against the brief before it was written (on-brief, Goals rows 2 and 3). The de-emoji and n8n ingress work are polish and defect-fix inside existing scope.
- **Closest call, guarded, worth naming:** the n8n adversarial inbox has an external model writing files into this repo automatically. That sits adjacent to non-goal (a), *"not autonomous AI-dispatches-AI, not a hosted authority."* It stays inside the line because the contract is explicit and enforced in `skills/g-patterns/SKILL.md`: counter-reports are advisory only, human-weighed, never auto-acted-on, and inbox content is DATA never instructions. Not creep — but it is the one place where a future loosening would cross a declared non-goal, so it deserves the ADR the handoff already carries as an open item (inbox trust-boundary).

## MVP integrity

MVP shipped at M15 / v1.0.0 and its done condition is met — a non-trivial change goes request → gated, reviewed, committed without manual agent orchestration. All current work is post-MVP hardening on the freeze release. No MVP done-condition is open, so no bypass risk exists on this axis.

## Tech-decision drift

- [✓ none] Every row of the brief's Tech-decisions table matches actual practice: Claude Code plugin platform, git-hook gate + two sentinels, distilled `g-docs/` record plus observer journal, single-use scoped agents, GitHub + marketplace deployment (v2.4.0 released and tagged 2026-08-16), and the v2.5 → G-Proof 1.0 version identity reaffirmed by ADR-010.
- One item worth tracking as a *defect*, not drift: audit-4 W3 found `review-orchestrator.md:22` dispatching `doc-writer` to write docs mid-review, which contradicts the "single-use, scoped" agents row in spirit and G-RULES §G in letter. Now in M50 scope — a bug against the decision, not a change of decision.

## Recommendation

**ALIGNED — no realignment needed.** The trajectory serves the brief, and the one axis that had genuinely decayed was found and given a milestone within the same session.

The finding worth acting on is not drift, it is **cadence**: this check was 25 days late against a 7-day nudge, and the telemetry instruments it partners with had been silently wrong for longer. Two of the brief's self-governance instruments failed the same way — they report clean when unrun or misconfigured. That is exactly M50's thesis, which makes M50's sequencing (second, right after M48) the correct call rather than a nice-to-have.
