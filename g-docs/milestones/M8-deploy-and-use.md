# M8 — Deploy & Use

## Goal
Self-host g-team on the g-team repo, add a `claude-plugin` stack profile, and add vibecoding skills for designing and validating Claude Code skills/agents.

## Scope
- [x] Install g-team into this repo (CLAUDE.md, hooks, settings.json, milestone files)
- [x] Create `milestones/M6`, `milestones/M7` files (retroactive)
- [x] `claude-plugin` stack profile — architect agent + architecture rules
- [x] `/g-skill-design` skill — guided workflow for designing new skills/agents
- [x] `/g-skill-validate` skill — validates SKILL.md and agent files against quality criteria
- [x] Register `skill-design` and `skill-validate` in `commands/g-team.md` router

## Done condition
`/g-doctor` reports 9/9 on this repo. `claude-plugin` profile installs cleanly via `/g-specialize`. `skill-design` and `skill-validate` skills are reachable via `/g-skill-design` and `/g-skill-validate`.

## Status
✅ Complete (v0.9.0)
