# M-audit-2026-07 — Forge Integrity: Full Codebase Audit

**Generated:** 2026-07-01
**Status:** ✅ Complete (W0–W3 + stdin-guard release rider shipped; v2.3.0 released 2026-07-23)
**Version:** v2.3.0 (minor — W1 is new enforcement capability, not fixes; upgraded from v2.2.2 — developer, 2026-07-18. Release pass at close ships the first README status strip + CHANGELOG `[2.3.0]` cut, starting the standing currency convention. Release-pass checklist item added 2026-07-22 W2 task 8: confirm at the first post-release consumer update that only the new version's entries register — no shim entries survive the versioned-cache replacement [ADR-007 migration risk].)
**Goal:** The enforcement layer provably enforces, and drift from that state is detectable. Resolve the 2026-07-01 three-agent audit findings, ordered by risk-adjusted priority.
**Risk level:** Medium — two P0 items are design questions (worktree gating, sentinel content-binding), not mechanical fixes.

---

## Wave 0 — Quick wins ✅ (merged `4158ffa`)

| # | Finding | Resolution |
|---|---------|-----------|
| 1 | Skill count "37" stale in README + marketplace | Corrected to 38 |
| 2 | `/g-update` Step 6 never synced g-rules-*.md section files | Step 6a added (10 sections, trim-respecting) |
| 3 | Hook matchers `Bash`-only — commit gate fail-open on Windows | `Bash\|PowerShell` in g-init scaffold + g-update table; tests 17–18 pin payload contract |
| 4 | `templates/roundtable/session-rules.md` + `summary.md` flagged dead | **False positive** — staged for M33 Phases B/C per spec A2/C2; keep |

---

## Wave 1 — P0: Enforcement integrity

| # | Finding | File | Severity | Impact | Change Risk |
|---|---------|------|----------|--------|-------------|
| 5 | Installed-copy drift undetectable — `.claude/hooks/` diverged from canonical `hooks/` (Bug A was live in this repo); no test or doctor check compares them | `skills/g-doctor/SKILL.md` (new check: hash-compare installed vs plugin source) | Critical | High | Low |
| 6 | Commit-detection misses `git -C <path> commit`, `git -c k=v commit`, aliases; same gap in cleanup + observer | `hooks/check-commit.sh:66`, `post-commit-cleanup.sh:44`, `observe.sh:80` | Major | High | Medium |
| 7 | `git commit -a` / pathspec commits bypass the staged-set classifier (code ships under doc sentinel) | `hooks/check-commit.sh:93` | Major | Medium | Medium |
| 5r | **Resolved:** finding #5's new drift check is a **required** `/g-doctor` check (not advisory) — drift is exactly the failure mode Wave 1 exists to make detectable, so it must count toward the pass/fail total like Checks 1–15, not sit alongside the 16–21 advisories. | `skills/g-doctor/SKILL.md` | — | — | — |
| 8 | Sentinel is content-blind — edits after MERGE READY still pass; not bound to reviewed tree hash | `skills/g-review/SKILL.md:135` + `check-commit.sh` (design: stamp diff hash into sentinel, verify at gate) | Major | High | Medium |
| 9 | Terminal commits (outside any matched tool) never clear sentinels → next in-session commit rides stale approval | `hooks/post-commit-cleanup.sh` (design: cleanup-on-check — gate compares sentinel timestamp vs HEAD commit time) | Major | Medium | Medium |
| 10 | Worktrees have no `.claude/` (gitignored) → all seven hooks silently off in `isolation: worktree` agent sessions | `.gitignore` + design spike — likely ADR (what *should* enforcement mean in a worktree?) | Major | High | High |
| 11 | Zero test coverage: post-commit-cleanup, pre-compact, session-start, agent-lifecycle; workflow-checkpoint only partially | `tests/` (new suites) | Major | High | Low |
| 21 | **New (BUG-2, reported 2026-07-13 G-Tools retro corpus; reproduced LIVE this session):** the commit gate false-*fires* — the classifier greps the raw command string for a `git commit` substring, so any command that merely *contains* that text is blocked though nothing commits (confirmed: a `for`-loop/`grep` whose quoted test string held `git commit` was denied; regex test shows `… && git commit -m z` fires). The `f55ccdb` boundary-anchoring fixed only the trailing-`"` sub-case. This is the false-**positive** counterpart to #6/#7 (false-negatives) and **cannot** be fixed by loosening the regex without reopening #6/#7's `-C`/`-c` evasions. Fix: stop grepping the raw string — anchor to real command boundaries (start / after `&&`/`;`/`\|`) or parse the invoked argv. Also inherent: PreToolUse sees pre-execution state, so a single chained call that writes the sentinel *then* commits can't be gated mid-chain. **Third surface (demonstrated LIVE 2026-07-13 while committing this very finding):** the pathspec isolator `sed`-slices everything after the `commit` keyword and tokenizes it, so a commit whose *message body* merely **cites** real file paths (e.g. `hooks/check-commit.sh` + a `g-docs/…` path) is tokenized as pathspecs → both a code and a doc path "match" → the commit is misclassified **mixed** and a fully doc-approved commit is blocked **by its own description**. Confirms the fix must parse the actual invoked argv, not string-slice the command. **Folded into the ADR-004 implementation pass — do not fix independently; see the Wave 1 coordination note + the "#21 fix spec" subsection below.** | `hooks/check-commit.sh:66` | Major | Medium (forces commit-splitting; blocks unrelated commands) | Medium |
| 25 | **New (found 2026-07-17, W1.5a fail-before attestation + HQ probes):** chain-evasion **regression** — the W1.2 argv swap lost chain detection: `is_git_commit` parses the whole string as ONE argv, so `cd x && git commit`, `…; git commit`, `…\| git commit` are all UNDETECTED, and `check-commit.sh:133` passes the raw string with no chain-splitting. The pre-swap regex DID fire on chains (see #21's history). Until W1.7 installs the native pre-commit hook, PreToolUse is the only enforcement → any chained commit bypasses the gate. No chain-positive test existed, so 61/61 stayed green through the regression. Root fix (W1.5a amended scope): lib-level segment splitting — split the token stream on `&&`/`\|\|`/`;`/`\|` and run the argv walk per segment; detection = any segment commits. Residual: the same-call sentinel-write-then-commit smuggle stays inherent to PreToolUse (already recorded in #21) — definitive close remains ADR-004's native hook. | `hooks/lib/commit-detect.sh` + `hooks/check-commit.sh:133` | Critical (until W1.7 install; then Major) | High | Medium |
| 26 | **New (found 2026-07-17, same pass):** single-char env-prefix evasion — the VAR=val strip glob `[A-Za-z_][A-Za-z0-9_]*=*` fails to match single-character names on the deployment platform's bash (git-bash 5.2.37: `A=1` NO-match, `AB=1` match; reproduced in both `case` and `[[ ]]`), so `A=1 git commit` is undetected. Same class pattern reused in the env inner loop. Root fix (W1.5a): restructure to a platform-safe check (e.g. split name via `${tok%%=*}` + validate) rather than relying on the quirky glob. **Also corrected in the same pass:** the tokenizer's unmatched-quote behavior is *partial tokens emitted → DETECTED* (fail-toward-deny, safe) on GNU xargs — not "no tokens → false" as the code comment and the 2026-07-14 minor claimed; comment + test pin polarity fixed. | `hooks/lib/commit-detect.sh:46-64` | Major | Medium | Low |

| 8r+9r | **ADR'd → [ADR-004](../decisions/004-bind-sentinel-to-reviewed-tree.md) (Accepted).** #9 folded into #8. Enforcement site moves to a **native git `pre-commit` hook** (index is authoritative → survives `commit -a`/`-p` and raw-terminal commits, covering #9 natively); PreToolUse kept for the model-facing deny. `/g-review` stamps `git write-tree` + HEAD sha; the hook re-derives and denies on tree-mismatch or HEAD-advance. **Review-flow follow-up resolved (2026-07-14) — see ADR-004's Resolution section:** `/g-review`'s primary target flips to `--staged` (+ unstaged-tracked union), `main...HEAD` demoted to fallback; scope is a one-line diff-target change in `skills/g-review/SKILL.md`. Remaining follow-ups before code: first-commit special-case, drift-safe rollout with the #5 check. | `skills/g-review/SKILL.md` + new `hooks/` pre-commit installer + `check-commit.sh` | — | — | — |
| 10r | **ADR'd → [ADR-005](../decisions/005-worktree-enforcement-semantics.md) (Accepted).** Inherit primary enforcement state via `git rev-parse --git-common-dir`; **per-worktree-keyed sentinels** (`show-toplevel`) to kill cross-tree approval bleed; gate-normally on resolution, deny only when primary state is unreachable; never silently no-op a gated worktree. Depends on ADR-004 (shared sentinel files). Follow-ups: exact shared-vs-per-tree state partition; deny-vs-gate ambiguity list; optional isolation escape hatch. | seven `hooks/*.sh` (one sourced resolver helper) | — | — | — |
| 8i+9i+10i+21i | **W1.2 SHIPPED (2026-07-15, `1621a70` + follow-up fix commit).** ADR-004/005 implemented: `check-commit.sh` swapped onto `hooks/lib/{commit-detect,worktree-resolve}.sh` + NEW `hooks/lib/classify-changeset.sh` (classifier extracted so ONE copy exists — coordination-note conformant; #21 surfaces #1/#3 dead at the PreToolUse site, existence-only sentinel checks preserved, exit-2 deny contract intact); native `hooks/pre-commit` created (authoritative site: write-tree/HEAD/worktree stamp verify, first-commit fail-toward-deny, mixed validates both sentinels, consume-on-pass; NOT installed/registered yet — W1.4). `g-doc-review` Step 1 staged-union flip closed 8d's residual. Review: r1 HOLD — Major: stamp parser truncated terminal `commit_sentinel_worktree` at first space (spaced Windows paths = permanent deny); fixed, fail-before/pass-after fixture. r2 MERGE READY. Minors carried: g-review SKILL Step 6 literal-HEAD wording (→W1.5 test pass), architecture-rule native-git-hook class note (→W2 #18). Sandbox-proven 19/19+6/6; suite 60/60. #21 stays OPEN until surface #2 is closed by W1.4's install (hook must actually run under git). ⚠ Process note: the W1.2 content itself reached main as an ungated raw-terminal commit (`seed`) — live demo of the #9 hole this wave's hook closes once installed. | `hooks/check-commit.sh`, `hooks/lib/classify-changeset.sh`, `hooks/pre-commit`, `skills/g-doc-review/SKILL.md` | — | — | — |
| 11i–18i | **W1.3 REVIEWED MERGE READY (2026-07-16, pending commit).** Six hooks worktree-integrated per ADR-005: `post-commit-cleanup.sh` (commit-detect.sh + worktree-resolve.sh; both `rm -f` sentinel paths via `GF_CLAUDE_DIR` mirroring `check-commit.sh`, ambiguity ⇒ exit 0 clearing nothing), `observe.sh` (commit-detect.sh; JOURNAL → PRIMARY `.claude/journal/` when linked, `wt`-tagged, primary untagged), `pre-compact.sh` / `session-start.sh` (guard local-first-else-primary, non-gating, other state stays local, primary byte-identical), `workflow-checkpoint.sh` (guard + review-sentinel read via one `GF_CLAUDE_DIR`), `agent-lifecycle.sh` (agent-log + mirrored journal → PRIMARY, `wt`-tagged). All 9 done conditions PASS (code-lead); single-classifier grep 0 across `hooks/`; primary-tree paths byte-identical; every worktree branch non-gating (degrade-silent, never deny). Suite 60/60. Prior implementer-flagged W2/W3/W6 "bugs" (agent-lifecycle/observe) re-confirmed **phantom** against live files. **4 Minor, none blocking:** (1) post-commit-cleanup lacks the sed command-field-extraction tier that check-commit.sh/observe.sh carry (harmless — ADR-004 content-binding invalidates any un-cleared stale sentinel) → W1.4; (2) observe.sh sed not escaped-quote-aware → W1.6; (3) W4 guard-idiom variance across the six → W1.5; (4) W5 workflow-checkpoint hand-rolls its own stamp parser — extract to `hooks/lib/` so the sentinel format gets a single reader (strongest advisory) → W1.5. Report: `g-docs/agent-output/review/code-lead-2026-07-16.md`. Tier 3 DoD: sandbox-proven; live verification → W1.7. | six `hooks/*.sh` | — | — | — |
| 25i+26i | **W1.5a SHIPPED (2026-07-18).** Root fixes landed in `hooks/lib/commit-detect.sh`: (#25) chain-segment scanning — `_commit_detect_scan_segments` splits the token stream on `&&`/`\|\|`/`;`/`\|`/`&` + newline-suffix walk in `_commit_detect_parse`; (#26) platform-safe `_commit_detect_is_var_assign` (split-then-validate, kills the single-char glob quirk); plus W1.1 minors — global-flag walk (`--git-dir`/`--work-tree`/`--namespace` value+glued, `--no-pager`/`-p`), `env -S` value re-tokenized/spliced (behavior fix, developer-approved), tokenizer comment polarity corrected (partial-tokens ⇒ DETECTED, fail-toward-deny). NEW `tests/test-commit-detect.sh` (43 tests; 14 fail-before KNOWN-BUG flips attested pre-fix). Review: **r1 HOLD (Critical)** — glued-operator forms (`x&&git commit`, `true\|git commit`, `echo hi;git commit`) bypassed: the splitter only saw operators xargs isolated as tokens; no glued-form fixture existed so 35/35 stayed green. **Fixed in-slice** (developer-approved): raw-string sed normalization pads bare `&`/`\|`/`;` BEFORE tokenizing — quoted operators re-unified by xargs, so commit messages never split; +7 fixtures (`KNOWN-BUG-glued-chains` + 2 regression pins). **r2 MERGE READY** (0c/0M/2m). Suite 104/104 across 7. Minors carried: (1) pathspec fidelity — a quoted filename containing a bare operator gets spaces injected; traced fail-safe (unmatched ⇒ CODE, the stricter gate) → W1.6; (2) env -S glued-operator residual **re-characterized: NOT a fail-open** — `env -S` does not interpret shell operators, those forms never commit. Newline-boundary dedicated test → W1.6 (standing no-re-accretion rule). Report: `g-docs/agent-output/review/code-lead-2026-07-17-w15a.md`. | `hooks/lib/commit-detect.sh`, `tests/test-commit-detect.sh` | — | — | — |
| w1.5b | **W1.5b SHIPPED (2026-07-18).** Two NEW suites: `tests/test-worktree-resolve.sh` (25 asserts — both public fns × primary/linked-worktree/spaced-primary/spaced-worktree/separate-git-dir-reject/non-git; relative-vs-absolute `--git-common-dir`; probe-first per W1.5a doctrine, HQ spaced-path gap-fill after agent hit the worktree-add-needs-`-b` quirk) + `tests/test-classify-changeset.sh` (42 asserts — every bucket rule + empty + mixed + single-classifier-invariant greps). Suite total 171/171 across 9. Two riders fired on fail-before evidence: **7a** — whitespace-only input FAIL adjudicated DOC-wrong-not-body-wrong (inline loop at `9688e95` byte-identical; behavior fail-toward-deny; comment-only header fix + test flipped, polarity precedent #26); **4a** — fixture flag-order bug (`-c` after subcommand) + a latent **false-green counter trap** (PASS/FAIL lost in subshells → `Results: 0/0`, always exit 0) restructured to parent-shell counting with a CANARY PROOF (corrupted copy → 24/1 exit 1). Review: MERGE READY r1 (0c/0M/3m). Minors → W1.6: (1) five CODE dir-arm fixtures use `.md` paths shadowed by the nested-`.md` arm (arms not independently exercised); (2) invariant grep scans `hooks/*.sh` only, missing `hooks/pre-commit`; (3, fixed pre-stamp) stale NOT-RUN banner. Report: `g-docs/agent-output/review/code-lead-2026-07-18-w15b.md`. | `tests/test-worktree-resolve.sh`, `tests/test-classify-changeset.sh`, `hooks/lib/classify-changeset.sh` (comment-only) | — | — | — |
| 1f | **Field-confirmed, not a new source bug (BUG-1, G-Tools 2026-07-13):** post-commit-cleanup left `.claude/g-forge-docs-approved` behind after doc/mixed commits — but **source is already fixed** (`hooks/post-commit-cleanup.sh:46` removes both sentinels, landed `f55ccdb`). G-Tools hit it through a **stale installed hook** → a live field instance of finding **#5** (installed-copy drift), the exact fail-open this milestone exists to make detectable. Remediation: `/g-update` in G-Tools; no source change. | `hooks/post-commit-cleanup.sh` | — | — | — |
| 8d | **DOC-sentinel stamp-drift (found + fixed 2026-07-15, `1e4d4a1`).** ADR-004's `git write-tree`+HEAD+worktree stamp was applied to the CODE sentinel (`/g-review` Step 6, `9688e95`) but **not mirrored to the DOC sentinel** — `skills/g-doc-review/SKILL.md` Step 4 was still writing the forgeable literal `approved`. Symptom in the field: the PreToolUse gate flagged the bash write of that literal as the exact forged-approval shape ADR-004 exists to catch, blocking the legitimate DOCS-READY sentinel write. **Fixed:** doc gate now writes the identical stamp format (byte-identical field names, verified by code-lead). **Residual follow-up (Minor, deferred):** `g-doc-review` Step 1 still detects the changed set with `git diff --name-only main...HEAD` as its *primary* target while the stamp binds the **staged** index — the same review-target-vs-gated-tree coherence gap ADR-004's 2026-07-14 resolution fixed for the CODE gate (flip primary to `--staged`+unstaged-tracked union, demote `main...HEAD` to fallback) but which was **never mirrored to the DOC gate**. Inert today (`check-commit.sh` gates on existence only); becomes load-bearing when W1.2's write-tree-verifying `pre-commit` hook lands. Mirror the Step 1 diff-target flip into `skills/g-doc-review/SKILL.md` in the same wave that wires the hook. | `skills/g-doc-review/SKILL.md` | Minor | Low (inert pre-hook) | Low |

**Done condition:** every P0 item resolved or ADR'd; new tests green; `/g-doctor` detects a deliberately-staled installed hook in a fixture. **#8/#9/#10 ADR'd (ADR-004, ADR-005) — verify both against the repo before writing W1 code. #21 (commit-gate false-positive, added 2026-07-13) needs a fail-before/pass-after test proving a non-committing command containing the `git commit` substring is NOT blocked — without reopening #6/#7's evasions.**

> **Wave 1 coordination — BUG-2 fold-in + consumer rollout (do NOT double-patch).** #21 is folded into the ADR-004 implementation pass, not fixed separately, because they touch the same file (`check-commit.sh`) and the same concept. Three compounding hazards to hold in one head:
> 1. **One coherent change, two enforcement sites.** ADR-004 keeps PreToolUse only for the model-facing deny message and moves authoritative blocking to a native `pre-commit` hook. #21's mis-detection lives in the retained PreToolUse classifier **and** must be mirrored in the new `pre-commit` detection — so the commit-detection logic must be a **single shared routine used by both sites** (or one test suite that pins both), never two hand-edited regexes that can drift apart. If #21 and ADR-004 are implemented as independent edits, the two sites will disagree on "what is a commit."
> 2. **Atomic consumer rollout.** These fixes ship only in source `hooks/`; a consumer (G-Tools et al.) receives them **atomically via `/g-update`**, never file-by-file. Guard against a half-updated hook set (new `pre-commit` present but PreToolUse stale, or the reverse) leaving the gate *worse* than before. The #5 drift check (`/g-doctor` Check 16) must hash the **whole** W1 hook set and flag any installed hook ≠ source; `/g-update` must sync the set together. Add a W1 test that a partially-staled hook set is reported as drift, not silently accepted.
> 3. **No downstream hand-patching.** A consumer that hot-fixes its own installed `check-commit.sh` to dodge BUG-2 will be (a) overwritten by the next `/g-update` and (b) flagged as drift until then. Workarounds go **upstream to G-Forge source**, never into an installed copy. **BUG-1 (1f) is the live example: G-Tools' correct fix is `/g-update`, not a local edit.** When BUG-2's source fix lands, G-Tools' remediation is likewise a single `/g-update` that pulls the coherent hook set — do not fix BUG-2 in G-Tools' installed copy.

### #21 fix spec (scoped 2026-07-13) — argv-parse commit-detection

⬜ spec only, not started. Executable by a fresh session / the ADR-004 pass. **Do not implement as an independent patch** — this defines the shared detection routine ADR-004's native `pre-commit` hook must reuse.

**Root cause — two string-op sites in `hooks/check-commit.sh`:**
- **Surface #1 (detection, `:66`):** matches a `git … commit` substring by command *content*, not by which command actually runs → fires on non-committing commands that merely contain `git commit ` followed by whitespace/args. Reproduced live on current `main` this session: a Bash `for`-loop whose quoted arg held `git commit -m z` was denied though it ran no git. (`f55ccdb` closed only the trailing-quote sub-case — `…git commit"` no longer fires — but `git commit <space>…` inside any string still does. The original `grep -iE "git add|git commit" …` from the BUG-2 report reflects a **pre-`f55ccdb` stale installed hook** in G-Tools, not current `main`.)
- **Surface #3 (pathspec extraction, `:127`):** `sed -E 's/^.*[[:space:]]commit([[:space:]]|$)//'` greedily slices after the **last** `commit` token and `xargs`-tokenizes the rest → a `-m` message body citing real paths is parsed as pathspecs → misclassified **mixed**.
- **Surface #2 (chained pre-exec state):** inherent to PreToolUse — `… && git add x && git commit` in one call is evaluated pre-`git add`. **Not fixable here; stays for ADR-004's `pre-commit` hook (index authoritative).**

**Fix — one sourced routine (`hooks/lib/commit-detect.sh`) reused by PreToolUse + ADR-004's `pre-commit` (no second regex):**
1. Identify the actual command: strip leading `VAR=val`/`env`; require the first real token to be `git` (bare or `…/git`). Not git → not a commit → **pass** (kills #1).
2. Confirm subcommand: walk argv skipping value-taking global flags (`-C <path>`, `-c <k=v>`); first non-flag token must be exactly `commit`. A quoted `commit` substring is never the subcommand token.
3. Pathspecs from argv only: skip option-values (`-m <msg>`, `--message=…`, `-F <file>`, `--author=…`); treat only bare positionals (after `--`/non-flag) as pathspecs — the `-m` value is never a pathspec (kills #3).
4. No whole-string `sed`-slice. If argv parsing is defeated (nested quotes/subshell), **fail safe toward gating** (stricter code bucket) — but never fire when the first command isn't git.

**Test plan (extend `tests/test-check-commit.sh`, fail-before/pass-after):**
- T-a #1 (regression guard — already passes post-`f55ccdb`): `grep -iE "git add|git commit" retros/*.md` (ends `commit"`) → stays NOT blocked. (The true fail-before for #1 is T-b, which fires on current `main`.)
- T-b #1/#3 (true fail-before on current `main`): `echo`/`for` whose quoted arg holds `git commit -m z` → currently **fires**; must NOT block after fix.
- T-c #3: `git commit -m "touches hooks/x.sh and g-docs/y.md"`, doc-only staged set + doc sentinel → classified **doc**, not mixed.
- T-d (no #7 regression): `git commit -- hooks/x.sh` real pathspec + code staged → still **code**.
- T-e (no #6 regression): `git -C /repo commit`, `git -c k=v commit` → still detected.
- T-f (core gate): bare `git commit`, code staged, no sentinel → still **blocked**.
- Existing 22-test suite stays green.

**Constraints:** shared routine mandatory; does **not** close BUG-2 fully (#2 waits for ADR-004 — never record #21 resolved until then); atomic `/g-update` rollout with #5 drift check flagging a partial hook-set update; gate self-modification → mandatory `/g-review` + full suite green with pasted output (finding #20 / g-rules-H). **Estimate:** ~1 wave (implementer + test-writer). Forward-compatible with ADR-004 — it *provides* the routine ADR-004 needs.

---

## Wave 2 — P1: Structural conformance + rule alignment

| # | Finding | File | Severity | Impact | Change Risk |
|---|---------|------|----------|--------|-------------|
| 12 | `argument-hint:` in SKILL.md frontmatter (9 skills: g-audit, g-adr, g-docs, g-help, g-optimize, g-refactor, g-roundtable, g-train, g-wiki) | each `SKILL.md:4` | Major | Medium | Low |
| 13 | Announce line missing/malformed (g-doctor bare text; g-help buried in Step 0; g-status inline in Step 1) | 3 skills | Major | Low | Low |
| 14 | `## Rules` section missing/malformed (g-brief, g-doctor; g-status uses inline bold) | 3 skills | Major | Low | Low |
| 15 | Step structure non-conforming (g-status `**Step N.**`; g-doctor `## Checks`) | 2 skills | Major | Low | Low |
| 16 | architecture-enforcer body verdict `PASS\|FAIL` vs return block `PASS\|HOLD` (Bug-C family, missed by v2.2.1) | `agents/architecture-enforcer.md:37` | Major | Medium | Low |
| 17 | `/g-forge` umbrella router missing `roundtable` subcommand row | `commands/g-forge.md` | Minor | Low | Low |
| 18 | Architecture rule stale: "agents are reviewers only, Read/Glob/Grep" predates diagnostic (code-lead/debugger/error-detective +Bash) and implementer (+Write/Edit/Bash) classes; `context:` frontmatter endorsed by g-rules-J but forbidden by the rule | `.claude/rules/architecture-claude-plugin.md` + `profiles/claude-plugin/` source — define agent-class taxonomy + `context:` carve-out | Major | Medium | Low |
| 19 | **New (discovered 2026-07-06, reported from a consuming project's live install):** every skill's `description` is hand-authored in up to three places — `commands/<name>.md` frontmatter, `skills/<name>/SKILL.md` frontmatter, and `commands/g-forge.md`'s own subcommand one-liner — and they've drifted (confirmed on `g-adr` — a real behavioral disagreement, not just wording — plus a 5-pair spot-check: g-retro, g-review, g-audit, g-plan, g-execute, all drifted). Proposed fix: retire the standalone `commands/<name>.md` shims, make `skills/<name>/SKILL.md` the sole authored source, generate/reference the `g-forge.md` subcommand line from it. **Verified (2026-07-19, `hllrm/G-Cash` live install, v2.2.1 cache) — reading (a) confirmed, `/g-adr` unblocked.** The paused question — whether two files mean (a) two independent visible entries, (b) a duplicate registration, or (c) Claude Code's inherent short-alias + `plugin:name` pair for a single registration — resolved as **(a)**: all three probed pairs (g-adr, g-review, g-plan) show **two visible entries under the same `g-forge:<name>` display name with *different* description text**, each tracing verbatim to a different source file (entry A = `commands/<name>.md`, entry B = `skills/<name>/SKILL.md`). (b)/(c) refuted — both would require identical text. The `g-forge.md` umbrella registers one entry of its own but adds no per-skill third entry. Same duplication independently observed on this repo's own dev install the same day. **New cost surfaced for the ADR to weigh:** deleting a shim also removes the bare `/g-<name>` slash alias — invocation then only via `/g-forge <name>` or the plugin-qualified name — so shim retirement is a real 2→1 reduction but not invisible to consumers. **Decided 2026-07-19 → ADR-007 (Accepted): retire all shims, SKILL.md sole source, umbrella trimmed to bare tokens; keep-and-sync options rejected (one command per thing, no added layers); migration free via versioned cache replacement. Sequencing: amend g-skill-validate/g-skill-design/architecture rule BEFORE deletion — see ADR-007 Consequences.** | `commands/*.md`, `skills/*/SKILL.md`, `commands/g-forge.md` | Major | Medium (correctness risk per g-adr) | Medium (structural — visible entries 2→1, bare `/g-<name>` alias removed for consumers) |

| 20r | **Resolved (2026-07-12, queued for v2.2.2 — on `main`, CHANGELOG `[Unreleased]`; manifest stays 2.2.1 until milestone close):** fix **A+C** shipped — `test-writer` returns `RESULT: WRITTEN` + `RUN STATUS: NOT RUN` and is barred from any pass claim (`agents/test-writer.md`); `/g-execute` treats `WRITTEN` as incomplete until HQ runs the suite (green→done, red→fix loop); `code-lead` + g-rules-H Tier 1 require runner output for any test attestation, so a self-declared done/written or a non-executing agent's result is UNVERIFIED and blocks MERGE READY. **Field corroboration (2026-07-14, G-Cash patch brief):** the identical pattern hit the first from-scratch consumer ≥4× in one build leg — test agents returned "DONE / ready / should pass" while the suite was red (broken mocks, wrong paths, fmt). Observed on G-Cash's **v2.2.1 installed copy, which predates this fix** — confirms severity and that the fix only lands for consumers at the v2.2.2 release + `/g-update`. (The G-Cash brief's other patches were scoped as roadmap **M42 — Planning Cold-Start Integrity**.) | `agents/test-writer.md`, `skills/g-execute/SKILL.md`, `agents/code-lead.md`, `rules/g-rules/H-testing.md` | — | — | — |
| 20 | **New (reported 2026-07-12 from a parallel session):** `test-writer` agents repeatedly returned `RESULT: DONE` while the suite was actually **RED** on independent verification (observed **4×**). Root cause: `test-writer`'s tools are `Read, Glob, Grep, Write, Edit` — **no `Bash`** — so the agent *cannot execute the suite it writes*; its `DONE` can only mean "test files written," yet HQ/code-lead consume it as "tests green / ready for execution." The return grammar (`RESULT: DONE\|FAILED\|BLOCKED`) conflates *authored* with *verified*, and requires **no evidence** (real runner output / pass-fail counts). This is a false-success signal defeating the review pipeline — the same integrity class as a gate that looks present but never fires. Fix fork (needs a call): **(A)** keep test-writer execution-free (least-privilege haiku) but relabel its return so `DONE` ⇒ "written, **UNVERIFIED**," and relocate the actual run to an explicit verification step (HQ or a dedicated runner agent) that records RED/GREEN with **pasted counts**; **(B)** give test-writer `Bash`, require it to run the suite and paste real pass/fail output, forbid `DONE` without a green (or explicit RED) run; **(C)** gate-level — `code-lead`/`/g-review` must mechanically confirm "tests executed green, output pasted" as a hard done-condition before MERGE READY, so a false `DONE` can't reach a sentinel. Recommend **A + C** (preserves single-use least-privilege; puts truth-enforcement at the gate, per doctrine). Cross-check g-rules-H Tier 1 ("any red = stop") — currently unenforced against agent self-report. | `agents/test-writer.md` (return grammar) + `agents/code-lead.md` / `skills/g-review/SKILL.md` (relocated run + evidence) + `rules/g-rules/H-testing.md` | Major | High | Low |

| 28 | **New (2026-07-19, DEFCON — the dogfood gap):** this repo develops vN while *running* vN−1 on every runtime layer. Measured: 7/7 installed hooks drift from canonical; `.claude/hooks/lib/` absent entirely (install predates W1.1); native `pre-commit` not installed; skills+agents resolve from the v2.2.1 plugin cache and drift from source (verified g-review, code-lead); zero of ten g-rules section files installed → `G-RULES.md` `@`-includes silently no-op, sections A–J never loaded in any session here. The W1.5f commit was gated by the v2.2.1 gate containing the very bugs W1.5a fixed. All three field bugs (BUG-1/#21/#22) were found by consumers because the canary reports on vN−1 by construction. Root causes: no mechanism installs from the working tree (`/g-init`/`/g-update` hardcode the cache as source root — running `/g-update` here would *revert* current hooks to v2.2.1, a footgun), and required Check 16 is in no routine loop (process gap). **DECIDED → ADR-008 (Accepted, two-way door): self-host from working tree; install cadence split by hook class (non-gating eager at slice close, gating clone-first at verified checkpoints); mechanism = self-host-aware `/g-update`+`/g-init` (root `plugin.json` name-match detection flips source root — kills the footgun structurally); #27 is PREREQUISITE (verification before installation); routine drift check in `/g-review` Step 1; non-gating never-exit-nonzero suite invariant; skills/agents layer is cache-pinned by construction (routers hardcode the cache Glob; `g-forge:` namespace unshadowable) → Spike S1 (local-marketplace dev-install path, 2 empirical questions). Implementation = W1.5g (inserted slice); gating installs stay W1.7.** Rollback contract in the ADR; `.claude/` snapshot taken 2026-07-19 (scratchpad). | `skills/g-update/SKILL.md`, `skills/g-init/SKILL.md`, `skills/g-doctor/SKILL.md`, `skills/g-review/SKILL.md`, `tests/` | Critical (process) | High (dogfooding claim false; consumer-first bug discovery) | Medium (shipped install-path change, gated + clone-first) |

| 27i+28i | **Implemented (2026-07-21, W1.5g Waves 1–5, commit `137e56a` — reviewed MERGE READY 0c/0M/1m, DOCS READY 0b/3w):** extended Check 16 shipped (10-file g-rules flat-rename mapping missing=Fail + three-class installed-agents rule — profile-copied hash-compare / template-instantiated advisory-only / project-local `*-dev.md` excluded); ADR-008 self-host detection live in `/g-update` Step 0/1 + `/g-init` Step 1a (verbatim-identical condition; cache Glob fallback-only); `/g-review` Step 1 routine drift check (visible-only, never gates); `tests/test-class-split-invariant.sh` NEW 26 assertions + drift suite 3→12 (total 245/11 suites, attested canonical-invocation). **First live self-host install succeeded:** 21 files (6 non-gating hooks + 4 libs + 10 g-rules + architect agent) byte-identical, HQ-verified; gating pair untouched (W1.7); post-install extended-Check-16 PASS 0 Fail. Residuals: Spike S1 (tasks 15–17) deferrable Pass 3; gating clone-first install = W1.7; sentinel auto-consume bypassed by script-file commit indirection (#21 residual class → W1.7 checklist). | shipped: `skills/{g-doctor,g-update,g-init,g-review}/SKILL.md`, `tests/` | — | — | — |
| W1.6i | **Implemented (2026-07-21, W1.6 all 7 waves — reviewed r1 HOLD 0c/3M/5m → fix wave → r2 MERGE READY 0c/0M/0m, attested 403/403 across 15 suites, header-vs-runner MATCH everywhere):** 4 never-tested hooks got behavioral suites (session-start 30, pre-compact 22, workflow-checkpoint 64, agent-lifecycle 29) + 15 carried minors closed. **Two REAL hook bugs found+fixed in-slice: F1** `session-start.sh` read only tier-file *existence*, never its value — banner fired on `light` where docs promise silence; fix: light exits before banner, state maintenance (counter reset) kept on all tiers. **F10** `workflow-checkpoint.sh` `PROMPT_COUNT_FILE` bare-relative → depth counter blind from linked worktrees; r1 caught the fix half-applied (reader resolved, writers not — would have flipped blind counter into unresettable false-red); completed all 7 counter-path sites (`session-start.sh` writers, `pre-compact.sh:46,57`, `workflow-checkpoint.sh:219-220,247-248`) + 3 new linked-worktree reset assertions, canary-proven. **HQ-found Critical missed by r1:** `test-agent-lifecycle.sh` false-green — `[ -d …/.git ]` guard on a linked worktree (where `.git` is a FILE) meant the whole wt-tagging section (task 7's headline condition) never ran while the skip branch credited 4 phantom passes; second dead branch (glob-in-assignment) silently dropped 6 pairing assertions; suite 21 reported (17 real + 4 phantom) → 29 real. One first-real-run failure adjudicated HOOK-CORRECT/fixture-side (Windows `show-toplevel` native spelling vs MSYS mktemp spelling + malformed `grep` compare). **r1 Majors 2+3:** non-discriminating pins (`approved` matched `not yet approved`; `^sedtier` matched both truncation forms) rewritten + canary-proven red-under-corruption; 3 stale suite headers corrected, headers now pinned as runner-observed counts (finding-#20 cross-check). **g-forge-dev granted Write (developer directive, Pass 1)** — heredoc report writes were permission-blocked; scoped in agent body to `g-docs/agent-output/` only; `.claude/agents/g-forge-dev.md` tracked, rides this commit. Route-forward minors → W1.7/W2: 5 headerless suites (check-commit, post-commit-cleanup, review-severity, roundtable-degradation, sentinel-read — #20 cross-check inapplicable); stale `session-start.sh:33-34` comment; `--exclude-dir=lib` breadth; bare `full` pattern at workflow-checkpoint:193; CLAUDE.md ".claude/ git-ignored" claim vs tracked g-forge-dev.md; suite wall-clock (fresh mktemp+git init per assertion — shared-fixture pass someday). | `tests/test-{session-start,pre-compact,workflow-checkpoint,agent-lifecycle}.sh` (new), `hooks/{session-start,workflow-checkpoint,pre-compact,agent-lifecycle}.sh`, `tests/test-{commit-detect,classify-changeset,observe,worktree-resolve,class-split-invariant}.sh`, `tests/README.md`, `skills/{g-review,g-doctor}/SKILL.md`, `.claude/agents/g-forge-dev.md` | — | — | — |
| W1.7i (partial) | **Implemented (2026-07-22, W1.7 Waves 1–8 — FIRST LIVE GATED COMMIT `1acd512`, code-lead MERGE READY 0c/0M/1m + DOCS READY 0b/1w, both minors fixed inline pre-stamp; attested 405/405 across 15 suites + gate fixtures 37/0 + 35/35, header-vs-runner MATCH):** clone-first fixture `g-dev/fixtures/clone-first-gate-verify.sh` (37 assertions, real commits in throwaway clones) attested GREEN as the hard gate → gating pair installed live (`check-commit.sh` + 4 libs → `.claude/hooks/`, 11/11 hash-match; native `pre-commit` + `lib/` → git hooks dir, clobber-guarded; install probe caught lib-missing **fail-closed** — deny proven before the lib set was added) → escape hatches proven in scaffolded clone → **Task 14: dual ADR-004 sentinels stamped (write-tree+HEAD+worktree), commit `1acd512` passed BOTH live gates (PreToolUse argv + native pre-commit stamp match), both sentinels auto-consumed on a normal argv commit** — the W1.5g/W1.6 manual-clear residual closed on this path. Also: stray empty fixture-identity probe commit `20ddfbe` ("filler", `t@t`, escaped a throwaway clone during Pass-1 debugging, pre-dated gate install) detected at Pass-3 re-entry and dropped via `reset --soft` (tree-identical, unpushed, reflog-recoverable). Waves 9–12 (worktree gated commit, script-indirection live verify, argv auto-consume characterization, ledger close + finding flips) in flight — full W1.7i row lands at Wave 12. **Release-gate decision (developer, 2026-07-22): v2.3.0 ships at FULL M-audit close (W1+W2+W3), superseding the 2026-07-18 "at W1 close" call — one milestone, one version.** | `g-dev/fixtures/clone-first-gate-verify.sh`, `.claude/hooks/`, git hooks dir, `hooks/{session-start,workflow-checkpoint}.sh`, `tests/` (7 suites), `CHANGELOG.md`, `g-docs/forecasts/w1-7-gating-install-live-verify.md` | — | — | — |
| W1.7ii (close) + 8r/9r/10r/21r/25r | **W1.7 CLOSED (2026-07-22, Waves 9–12).** **Task 15:** gated commit from linked worktree `a966d88` — ADR-005 LIVE: doc sentinel stored primary-side keyed `commit_sentinel_worktree=D:/SW_Projects/G-Forge-wt-w17`, matched + consumed by the native hook from inside the worktree; bonus deny first: a pathspec-less `git commit` argv was denied by PreToolUse (unclassifiable ⇒ CODE fail-toward-deny, the pinned polarity) — doc-only commits signal class via explicit pathspec argv. **Task 16:** script-indirection deny-half PROVEN — unstamped `--allow-empty` commit via script file (argv-invisible to PreToolUse, which passed the `bash` call) denied by native pre-commit rc=1, HEAD unmoved, matching clone-first evidence; pass-half = the commit carrying THIS row, executed via stamped script indirection (consume verified post-commit). Meta-proof: the first probe attempt was denied at PreToolUse because the heredoc BODY contained a commit line — the #21-residual heredoc/quoting false-positive class RE-CONFIRMED live; W2 look stands. **Task 17:** satisfied by `1acd512` + `a966d88` — normal-argv commits, sentinels absent post-commit, zero manual `rm` (native-hook consume-on-pass; the W1.5g/W1.6 manual-clear residual closed). **Task 19 (#22 live):** journal now attributes agents — `2026-07-21.jsonl` (`g-forge-dev start/stop a58bfb93`), `2026-07-22.jsonl` (`g-forge:code-lead stop ae0736b5`, `g-forge:doc-reviewer start/stop aa479e00`/`a814be99`) — no `unknown`, RESULT parsing live. **Finding flips (Task 27):** **#8 CLOSED** (content-bound sentinel live: stamped pass ×3, unstamped deny ×2); **#9 CLOSED** (native hook fires on every commit path — script/terminal included — and consumes on pass); **#10 CLOSED** (worktree enforcement live via `a966d88`); **#21 argv surfaces 1+3 CLOSED**, heredoc-content residual OPEN → W2; **#25 CLOSED** (chain-split + native hook = the definitive close the spec named). **Residuals routed (Task 28):** session-open multi-fire = platform-expected, journal `source`-field polish → W2/W3; suite wall-clock shared-fixture → backlog; NEW — PostToolUse skipped on rc≠0 tool calls: gated commits wrapped in failing chains go unjournaled + uncleaned by the argv sites (native consume covers); precise characterization → W2/W3; probe hygiene rule: never chain proof-steps into commit commands. `.claude/journal/payload-capture.jsonl` W1.5f debug leftover — delete or keep as fixture, developer's call. **Release gate: v2.3.0 at FULL M-audit close (W1+W2+W3) — recorded on the W1.7i row.** | `g-docs/milestones/M-audit-2026-07.md`, `g-docs/plans/w1-7-gating-install-live-verify.md`, `g-docs/ROADMAP.md` | — | — | — |
| 27ii+28ii (W1.5g close) | **W1.5g CLOSED (2026-07-22, Pass 3 = Waves 6–7) — Spike S1 executed, W1 COMPLETE (all slices W1.1–W1.7 + W1.5a–g + W1.6 shipped).** ADR-008's open follow-up (skills/agents dev-install path) answered empirically, decision-input only: **S1(a)** local-marketplace `add` under the same name REPLACES the registry entry (no side-by-side); `install` onto an existing same-scope install is a NO-OP (zero cache mutation — forced refresh requires uninstall-then-install); marketplace remove auto-removes its cache dir; GitHub install survived byte-intact (hash-verified). **S1(b)** router cache Glob with multiple version dirs is **mtime-ordered, not semver-ordered** — silently non-deterministic wrt version; edge state only (cache holds one dir in practice), but any future dev-install mechanism must never let two version dirs coexist. Cache layout note: g-forge = semver dirs; content-hash-dir marketplaces (caveman) can't hit this ambiguity. State restore HQ-verified (cache `2.2.1`-only, marketplace registry → GitHub; expected metadata-only `lastUpdated` diff). **Nothing decided/installed — cache-pinned skills/agents distribution stands; any future flip is its own gated slice.** Report: `g-docs/forecasts/w1-5g-spike-s1-skills-agents-devinstall.md` · probe record: `g-docs/agent-output/wave-w15g-6/`. | `g-docs/forecasts/w1-5g-spike-s1-skills-agents-devinstall.md`, `g-docs/plans/w1-5g-self-host-integrity.md` (Progress) | — | — | — |
| 19i (ADR-007 EXECUTED) | **Implemented (2026-07-22, W2 Waves 1–5, 4-pass split — plan `g-docs/plans/m-audit-w2-shim-retirement-conformance.md`):** the amend-first sequencing constraint honored — Wave 1 amended `/g-skill-validate` (shim presence now ✗, router bare-token check), `/g-skill-design` (SKILL.md-only authoring + one bare-token router line), and the architecture rule's `commands/` definition BEFORE deletion. Wave 2: **all 38 standalone command shims deleted** — `commands/` = `g-forge.md` only; 14 remaining refs classified (historical/infrastructural, 1 live teaching hit → Wave 3). Wave 3: teaching-docs sweep (66-row enumeration; README + g-help/g-kickoff/g-onboard refs → surviving `/g-forge <token>` form; rhetorical/historical untouched) + g-help unknown/retired-token branch. **Wave-5 walkthrough (task 16) caught the LAST live surface: the umbrella's fallback prose list (37 per-skill description lines) had survived Wave 1's "trimmed" claim — only the intro line had been reworded. HQ-deleted; router now 49 lines, bare tokens only, zero per-skill prose.** `/g-skill-validate` walkthrough post-fix: g-status/g-doctor/g-adr all VALID (full checklists + probe attestation `g-docs/agent-output/wave-w2-5/task16-skill-validate-walkthrough.md`). Post-release migration-check line on the release checklist (task 8). The three-surface description drift class is structurally dead: one authored source per skill. | `commands/` (38 deletions + `g-forge.md`), `skills/{g-skill-validate,g-skill-design,g-help,g-kickoff,g-onboard}/SKILL.md`, `README.md`, `profiles/claude-plugin/rules/architecture.md` | — | — | — |
| 12i+13i+14i+15i (conformance) | **Implemented (2026-07-22, W2 Waves 3–4):** structural conformance closed against the amended rule. `argument-hint:` removed from all 9 flagged skills (g-docs, g-refactor, g-audit, g-wiki, g-optimize, g-adr, g-help, g-train, g-roundtable — grep `^argument-hint` = 0 each). Announce line added/normalized in g-help/g-status/g-doctor; `## Rules` added in g-status/g-doctor/g-brief; `## Step N —` structure in g-status/g-doctor. Semantics unchanged — structural only. Task-9 authoritative recount (17 violation instances / 12 skills, 13 prior "violations" dissolved under the `context:` carve-out) fully discharged: every instance covered, zero gaps. | `skills/*/SKILL.md` (12 files) | — | — | — |
| 16i | **Implemented (2026-07-22, W2 Waves 1–2):** architecture-enforcer body verdict aligned to the orchestrator contract (`PASS\|FAIL` → `PASS\|HOLD`, Bug-C family). Attested `test-review-severity.sh` 9/9, header-vs-runner MATCH (g-forge-dev). | `agents/architecture-enforcer.md` | — | — | — |
| 17i | **Implemented (2026-07-22, W2 Wave 1):** `roundtable` row added to the umbrella router (frontmatter subcommand list + argument-hint + routing line). | `commands/g-forge.md` | — | — | — |
| 18i | **Implemented (2026-07-22, W2 Waves 1–3):** architecture rule rewritten — single-router `commands/` definition (ADR-007), two-class hook-contract note (#18: plugin hooks stdin-JSON/exit-2 vs native git hooks stderr/exit-1 — the fail-open Bug-A class), three-class installed-agent taxonomy (profile-copied / template-instantiated / project-local), `context:` frontmatter carve-out per g-rules-J. Eager-installed per ADR-008 non-gating cadence: installed copy byte-identical to source (blob `417f2ad`), drift suite attested 12/12 (g-forge-dev). Teaching example fixed (claude-plugin-architect.md:61 — ANY `commands/<name>.md` file = the violation). | `profiles/claude-plugin/rules/architecture.md`, `.claude/rules/architecture-claude-plugin.md`, `profiles/claude-plugin/agents/claude-plugin-architect.md` | — | — | — |
| 21iii (heredoc residual) | **Implemented (2026-07-22, W2 Waves 1–3):** the #21 heredoc-content false positive — the last open #21 surface, 3 live incidents on record — characterized then fixed. Characterization (task 18): misfiring branch = the newline-suffix walk (`commit-detect.sh:261-281`) had no heredoc awareness; quoting can't protect because line-splitting precedes quote context. Fix (task 19): new `_commit_detect_strip_heredocs` strips well-formed heredoc bodies before the suffix walk, guarded — **never** strips `bash`/`sh`/`eval` heredocs (body executes — that's W1.7's proven script-indirection deny path), unterminated heredocs left untouched (fail-toward-deny). 9 new HEREDOC fixtures; attested commit-detect **58/58** + check-commit **22/22**, header-vs-runner MATCH (g-forge-dev). Report writes citing commit phrases no longer denied; executable heredoc commits still gated. | `hooks/lib/commit-detect.sh`, `tests/test-{commit-detect,check-commit}.sh`, `g-docs/agent-output/wave-w2-1/heredoc-characterization.md` | — | — | — |
| 22ii (source-field polish) | **Implemented (2026-07-22, W2 Waves 1–3):** the W1.7-routed residual — observe.sh now journals the SessionStart `source` field (startup/resume/compact/clear), pinned by 6 new assertions; the session-open multi-fire anomaly is now diagnosable from the journal itself (platform-expected per W1.7 characterization: one fire per source event). Attested observe **30/30** + agent-lifecycle **29/29**, header-vs-runner MATCH (g-forge-dev). | `hooks/observe.sh`, `tests/test-observe.sh` | — | — | — |
| W2-close (PostToolUse boundary) | **W2 CLOSED (2026-07-22, Pass 4).** Final residual characterized (task 21): PostToolUse hooks are **platform-skipped when the tool call exits non-zero** — commits buried in failing chains go unjournaled and argv-side sentinel cleanup doesn't fire. Fixture `g-dev/fixtures/posttooluse-skip-boundary.sh` 6/6 proves the hooks are correct given a payload — the gap is the platform skip, not hook code. **Decision: ACCEPT no-fix** (native pre-commit consume covers the gate; probe-hygiene rule recorded in tests/README.md: never chain proof-steps into commit commands). ROADMAP W2 bullet rewritten to true scope (task 22). All 24 plan tasks complete; zero commits until this pass's single gated commit — the first to carry ALL W2 edits through the live gate. | `g-dev/fixtures/posttooluse-skip-boundary.sh`, `tests/README.md`, `g-docs/ROADMAP.md` | — | — | — |
| 27 | **New (2026-07-19, discovered live during a /g-patterns apply pass on this repo's own install):** `/g-doctor` drift detection is blind to installed **rules and agents** copies — required Check 16 hashes `.claude/hooks/` vs canonical `hooks/` only. Nothing verifies that the 10 `.claude/rules/g-rules-*.md` section files exist or match `rules/g-rules/`, nor installed `.claude/agents/` vs their shipped/profile sources. A missing rules file makes `G-RULES.md`'s `@`-include resolve **silently to nothing** — demonstrated live: this machine's install has zero g-rules section files (only `architecture-claude-plugin.md`), so G-RULES sections A–J never load into any session here, and `/g-doctor` would report healthy. Same silent-fail-open class as #5, consumer-facing (`/g-init` Step installs and `/g-update` Step 6a syncs exactly these files). Fix: extend Check 16 (or add a sibling required check) to hash-compare `.claude/rules/g-rules-*.md` and installed agents against canonical sources — a **missing** file counts as drift, not only a changed one. **Reclassified 2026-07-19: symptom of #28; PREREQUISITE task inside W1.5g (verification must precede installation — ADR-008 §4).** | `skills/g-doctor/SKILL.md` | Major | Medium (rules layer silently absent — session doctrine not loaded, undetectable) | Low |

| 22 | **New (BUG-3, reported 2026-07-13 G-Tools retro corpus):** the silent observer journals every agent dispatch as generic `{"kind":"agent","detail":"unknown start\|stop"}` — no agent type, task slug, or RESULT. `agent-lifecycle.sh` extracts the name via `.agent_name // .name // .subagent_name // .type` (all resolve to `unknown` — payload-field mismatch and/or python3/jq/node fallback) and records only `"$AGENT $EVENT"`. Effect: `/g-retro` and `/g-patterns` can't mine agent-level signal (which agent failed, redispatch counts) — the learning-loop evidence base is degraded and retros reconstruct agent activity from git + memory. Fix: confirm the real SubagentStart/Stop payload field names, capture `subagent_type` + task slug + RESULT into `detail`. **Supersedes/upgrades** the Wave 3 `agent-lifecycle … agent_type key probe` bullet. **Also verify while fixing (observed 2026-07-13 in a full-day journal — 187/187 events `unknown`):** (a) **start/stop imbalance** — 102 `start` vs 85 `stop` (17 orphaned starts with no matching stop); (b) **session-open multi-fire** — 3 `session opened` events within 7s (and 2 within 5s), a possible residual of the double-firing that `a57fcf9` "single registrar + self-guard" was meant to close. Both are unverified anomalies, not confirmed separate bugs — check them as part of #22's hook re-examination, since the fix must revisit when/how the lifecycle hook fires anyway. | `hooks/agent-lifecycle.sh`, `hooks/observe.sh` | Major (data-quality) | Medium | Low |

**Done condition:** `/g-skill-validate` passes on all 38 skills; agent taxonomy documented; combo-profile layer-map exception stated. Finding #19: live-install behavior confirmed and `/g-adr` finalized before any file is touched. Finding #20: test-writer return grammar no longer conflates authored-vs-verified, and a green run with pasted evidence is a mechanical done-condition at the review gate. Finding #22: agent events journal `agent_type` + short `agent_id` (the correlation key — the real payload carries no task-slug field) + RESULT (verified against a real SubagentStop payload), not `unknown`. **RESOLVED 2026-07-19 — pulled forward into W1.5f (developer order): fix shipped in `hooks/agent-lifecycle.sh` against live-captured payloads; `unknown` now reserved for absent/unparseable payloads, internal agents labeled `internal`; pinned by 6 new test-observe assertions. Start/stop imbalance explained (internal empty-agent_type stops, no matching start — not a bug); session-open multi-fire is registration-side, not in-hook (residual check → W1.7).** Finding #27: `/g-doctor` flags missing or drifted installed g-rules section files and agents, not just hooks.

---

## Wave 3 — P2: Minors (deferrable — execute only if Waves 1–2 come in under scope)

session-start resume-counter reset (A7 gate late) · agent-lifecycle JSONL escaping + tier-gating inconsistency + `agent_type` key probe · observe.sh control-char escaping / UTF-8 truncation · `README*` classifier path-prefix over-match · hardcoded plugin-cache path in workflow-checkpoint · per-project (not per-session) prompt counter · macOS `timeout` absence in session-start · combo-profile layer maps · **#6b (deferred from W1 #6): full `.git/config` `[alias]` resolution in commit-detection** — W1 hardened `-C`/`-c` global flags only; a user-defined git alias that expands to `commit` still bypasses the gate. Bigger change (hooks must read git config + resolve recursively); fixtures need a defined alias. Spike-or-scope on its own. · **#23 (BUG-4, 2026-07-13): `/g-doctor` Check 21 stray-doc scan uses a fixed dir-name allowlist** (`decisions\|retros\|forecasts\|telemetry\|blast-radius\|alignment`), so a parallel `docs/` tree (`agent-output/`, `plans/`, `qa-scope/`, `feedback-backlog.md`) slips through — widen the list or invert the check to "flag any G-Forge-authored doc outside `g-docs/`/`g-wiki/`." Minor. · **#24 (BUG-5, 2026-07-13): computer-use `request_access` can't target unregistered/running binaries** — a freshly-built Tauri `g-tools.exe` (running, correct title, confirmed via `tasklist`) couldn't be granted, blocking native-app Tier-3 verification that g-rules-H assumes is possible. Harness limitation, **not G-Forge code**; the actionable slice is a doc-gap fix — add a testing-protocol fallback in `rules/g-rules/H-testing.md` (native Tauri smoke tests need the app installed **pre-session**, or run in dev-mode). Enhancement / doc-gap.

| # | Finding | File | Severity | Impact | Change Risk |
|---|---------|------|----------|--------|-------------|
| W3i (close) + 6b-defer + 23i + 24i | **W3 CLOSED (2026-07-22, 3-pass split — plan `g-docs/plans/m-audit-w3-p2-minors.md`, 19 tasks / 4 waves / 13 slots, NOTHING deferred per developer directive "never skip issues — fix everything").** All Wave-3 minors executed in full: commit-detect gained the zero-cost `-c alias.NAME=VALUE` argv sub-case (`git -c alias.co=commit co` DETECTED) + heredoc opener-line **suffix**-scan (`cat <<EOF \| bash` with commit body DETECTED; interpreter-guard never-strip invariant intact) — **#6b config-file `[alias]` case formally DEFERRED per spike evidence** (`g-docs/agent-output/wave-w3-1/spike-alias-resolution.md`: `git config` read ≈230–260ms on EVERY tool call is a hot-path blocker; native pre-commit backstops aliased commits — residual is deny-timing UX only; developer-resolved 2026-07-22); **#23 CLOSED** — g-doctor Check 21 flipped from fixed dir allowlist to inverted self-deriving check (canonical names derived from g-docs/ itself, false-positive guard); **#24 CLOSED** — H-testing Tier-3 native-app fallback paragraph (install pre-session or dev-mode); session-start `resume` source now PRESERVES the depth counter (developer decision — A7-consistent with compact; startup/clear still reset) + `command -v timeout` guard; workflow-checkpoint plugin-cache path → highest-version-dir resolution + prompt counter session-scoped (`session_id`-keyed, concurrent sessions no longer clobber); agent-lifecycle + observe C0 control-char strip, observe UTF-8-boundary-safe truncation, tier-gating idiom aligned; classifier `README*\|CHANGELOG*\|LICENSE*` root-vs-nested split (narrowing-only, fail-toward-deny intact); g-onboard/g-kickoff rhetorical refs → `/g-forge <token>` (grep 0); 7 dual-stack combo profiles gained explicit two-stack layer-map/seam sections. **Attestation: 468/468 across 15 suites + 82/82 across 4 fixtures, header-vs-runner MATCH everywhere** (deltas: agent-lifecycle 29→35, observe 30→33, g-doctor-drift 12→17, commit-detect 58→65, classify 47→50, session-start 30→43, workflow-checkpoint 66→77); Wave-1 divergent "425/14" claim REFUTED by runner table; the attestation agent's own summary totals (568/650) were arithmetically wrong — HQ-corrected on the record after live spot-checks (claim-vs-data recurrence → /g-patterns). **Review: code-lead MERGE READY ×2 convergent** (r1 stalled on a heredoc record write post-verdict, killed; r2 fresh with inline-findings return — 0c/0M, 2 minors → backlog: per-session counter-file accumulation, r1 perf note). NEW field feedback captured mid-gate: review-pipeline cost scaling + code-lead orchestration shape + stall pattern → `g-docs/milestones/M36-salience-inputs/2026-07-22-review-cost-scaling-feedback.md` (intake after commit). | `hooks/{agent-lifecycle,observe,session-start,workflow-checkpoint}.sh`, `hooks/lib/{commit-detect,classify-changeset}.sh`, `tests/` (7 suites), `skills/{g-doctor,g-onboard,g-kickoff}/SKILL.md`, `rules/g-rules/H-testing.md`, `profiles/*/rules/architecture.md` (7 combos) | — | — | — |

---

## Execution notes
- Run `/g-plan` per wave; Wave 1 items 8–10 need design decisions before implementation — spike/ADR first, then implement.
- High change-risk item 10 (worktree gating) must not be "fixed" mechanically — it is a semantics question.
- P0/P1 must pass `/g-review` before Wave 3 begins.
- **Wave 1 mechanical items — Sub-plan A: MERGED to `main` (2026-07-06, fix `f55ccdb` + merge `7e9d9af`), reviewed MERGE READY (code-lead R2, all axes PASS), 60/60 tests green.** Planned (`g-docs/plans/m-audit-w1-enforcement-fixes.md`, 15 tasks / 5 waves) and executed on branch `fix/m-audit-w1-enforcement`: **#5** g-doctor required Check 16 (installed-copy drift, portable hash cascade; advisories → 17–22, total N/16), **#6** commit-detection hardened for `-C`/`-c` across the 3 hooks, **#7** classifier handles `-a`/`--all` + explicit pathspec. Each fix pinned by a fail-before/pass-after test; new `tests/test-post-commit-cleanup.sh` + `tests/test-g-doctor-drift.sh`. **#8–10 still excluded (spike/ADR first).** Decisions locked: Check 16 additive-required · cascade sha256sum→shasum→cksum · `-C`/`-c` flags only (full `.git/config` alias resolution DEFERRED — needs its own finding). NEXT: `/g-review` → commit → v2.2.2, then Sub-plan B (test-coverage tasks 16–21) in a fresh session.
- **Original plan note (superseded):** the earlier `/g-plan` on #5/#6/#7/#11 was paused mid-decompose when finding #19 surfaced; it was re-run cleanly on 2026-07-06 (17→15 tasks after scoping), so the paused scratch plan is moot. Finding #19 (Wave 2): `hllrm/G-Cash` live-install verification **done 2026-07-19** — reading (a) confirmed (see the #19 row); `/g-adr` unblocked.
