# Retro: adr007-w15e — 2026-07-19

## What was done
- **Finding #19 resolved end-to-end → ADR-007.** The paused registration question was settled by the developer-run `hllrm/G-Cash` live-install verification (two files = two visible entries confirmed; duplicate-registration and alias-pair readings refuted with verbatim-text tracing). `/g-adr` resumed: off-context deliberation surfaced an unnamed pointer-stub middle option and flagged the cache-removal assumption; the developer rejected every keep-and-sync variant ("one command per thing, one only — no added layers") → **ADR-007 Accepted** (`g-docs/decisions/007-one-command-per-skill-retire-shims.md`): retire all shims, SKILL.md sole authored source, umbrella trimmed to bare tokens, migration free via versioned cache replacement. Implementation = M-audit W2 with a hard sequencing constraint (amend g-skill-validate/g-skill-design/architecture rule before deletion).
- **Naming/version strategy downgraded from ADR to a brief row** (developer call: product strategy, not architecture). Flags struck in M44.md Rollback + the handoff; row added to `g-docs/project_brief.md` Tech decisions.
- **W1.5e shipped (`4e1c49a`).** Full cycle in-session: `/g-plan` (5 tasks / 2 waves, consolidation precedent applied) → `/g-forecast` (55% Elevated, calibration caveat on the doc) → `/g-execute` (3 Wave-1 dispatches all DONE first attempt; wave-close integrity clean) → HQ-run attestation 187/187 + 35/35 + 4/4 → `/g-review` MERGE READY (0c/0M/1m) → `/g-doc-review` DOCS READY (0 blocking / 1 warning, fixed pre-commit). Content: g-review Step 6 ↔ Step 2 reconciled (`--verify HEAD` + explicit `git add -u` union staging, independently validated against the hook's write-tree re-derivation on all three commit paths); Step 1 generalized to the project-local test-runner convention (generic-text grep 0); g-init `<git-hooks-dir>` warning; post-commit-cleanup dual-sentinel header (comment-only, 6/6 held).
- **CHANGELOG archaeology:** the finding-#20 bullet header had been silently destroyed in the W1.5d doc-writer overreach event — the prior retro's "single line, reverted, intact" understated the damage. Header restored from `22ea4f3`; W1.5e `[Unreleased]` entry added; M-audit milestone Version field synced v2.2.2 → v2.3.0 (doc-gate warning, pre-existing).

## Decisions made
- **ADR-007 (Accepted, two-way door)** — evidence: the ADR file, ROADMAP #19 text, M-audit #19 row, all committed in `4e1c49a`.
- **Naming/version strategy is not an ADR** — evidence: M44.md Rollback edit, project_brief.md new row, handoff note (`4e1c49a`).
- **W1.5e plan approved at 55% Elevated with mitigations pre-folded** — evidence: `g-docs/forecasts/w1-5e-skill-layer-edits.md`, plan file (gitignored local).
- **Attestation fallback reaffirmed** — g-forge-dev dispatch killed by session limit (2nd occurrence) → HQ ran the suite directly per the W1.5a precedent; verbatim record at `g-docs/agent-output/wave-w15e-2/full-suite-attestation.md` (gitignored local).

## Patterns
### Worked well
- **The no-child-dispatch hard line held.** Forecast scenario 1 (doc-writer overreach, 2 prior instances) did not fire — Wave-1 prompts carried an explicit child-dispatch prohibition and the wave-close integrity check showed only the 3 scoped files changed. First slice since the class appeared with a clean overreach record.
- **The whole-file coherence mitigation caught a latent contradiction.** The Step 6 implementer found and fixed a stale "currently-staged index equals reviewed tree" claim beyond the review-flagged sentences — forecast scenario 2 surfacing and being absorbed by the done condition rather than surviving to review.
- **Installed hooks livelier than the standing note claims.** Both sentinels were consumed automatically at `4e1c49a` (first time in 3+ sessions on this machine) and the installed PreToolUse gate demonstrably fired during the session. The "Windows fail-open, clear by hand" carry-over is at least partially stale — verify precisely at W1.7 `/g-doctor` rather than trusting either direction.
- **Doc gate + code gate both zero-blocking on first pass;** the doc gate's one warning (stale milestone Version field) was a pre-existing miss from the restructure commit, caught exactly where the gate should catch it.

### Avoid / do differently
- **Overreach damage audits must diff the whole file, not the noticed line.** The W1.5d doc-writer event destroyed the finding-#20 CHANGELOG bullet header *in addition to* the count edit that was noticed and reverted; the damage went unrecorded for a full session. When an agent edits out of scope, the recovery step is a full-file diff against git, not a spot-revert. (Second data point for the overreach class → /g-patterns.)
- **`#21`-class false-positive still biting in new forms:** the installed PreToolUse gate denied code-lead's *report write* — a heredoc merely quoting commit phrases — so the review record had to be persisted by HQ. W2 look queued (commit-detect on non-commit commands quoting the phrase).
- **Session-limit kills on long attestation dispatches (2nd occurrence)** — the g-forge-dev runner died mid-suite again; HQ fallback works but burns HQ context. Consider running the terminal attestation early in a fresh window (W1.5f plans the terminal sweep — schedule accordingly).
- **Finding #22 persists:** 60/60 agent events today are `unknown start/stop`; 20 session-open events show the multi-fire anomaly again. Attribution in this retro is from HQ context + git, not the journal.

## Cold-start context
**Branch:** main
**Active milestone:** M-audit-2026-07 — Forge Integrity (🔄 in progress; ships v2.3.0 at W1 close)
**Next up:** ⚠ FIRST: `/g-plan` W1.5f — guard-idiom normalization across the six W1.3 hooks + terminal full-suite attestation — then W1.6 (⚠ oversized, expect split) → W1.7 (live verify, first real run of stamped-sentinel + native pre-commit, local /g-update, v2.3.0 release pass incl. first README status strip + CHANGELOG cut).
**Key files touched:** SKILL.md (g-review, g-init), post-commit-cleanup.sh, CHANGELOG.md, ROADMAP.md, M-audit-2026-07.md, M44.md, project_brief.md, 007-one-command-per-skill-retire-shims.md, w1-5e-skill-layer-edits.md (plan+forecast), 2026-07-18-w15cd-capstone-restructure.md
**Carry-over context:** main @ v2.2.1, 8 commits ahead of origin (unpushed — developer's discretion). W1.5a–e shipped; standing rule: minors route to W1.6/W2. ADR-006 implementation post-M-audit; ADR-007 implementation = W2 (verify ADR-007 from a fresh window before W2 executes). stash@{0} superseded, droppable. Installed-hook liveliness partially contradicts the Windows fail-open note — verify at W1.7. Arc: M-audit (v2.3.0) → M41 (v2.4.0) → … → M44 G-Proof 1.0 capstone.

## Journal basis
83 events today: 60 agent (all `unknown` — finding #22) · 20 session (multi-fire anomaly visible) · 3 commit. Attribution drawn from HQ context + git, not the journal.
