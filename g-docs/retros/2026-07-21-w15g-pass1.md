# Retro: w15g-pass1 — 2026-07-21

## What was done
- **W1.5g Pass 1 (Waves 1–3) executed end-to-end** from a fresh session (/g-resume → /g-execute), per the approved planned split. Zero commits by design — all work rides to the post-Pass-2 /g-review commit.
- **g-doctor Check 16 extended in place** (finding #27): 10-file g-rules flat-rename mapping (`rules/g-rules/X-name.md` → `.claude/rules/g-rules-X-name.md`, missing = Fail, same hash cascade) + three-class installed-agents rule (profile-copied hash-compare / template-instantiated advisory-only / project-local `*-dev.md` excluded).
- **ADR-008 self-host detection mechanism shipped** into /g-update (Step 0/1) and /g-init (new Step 1a) with verbatim-identical condition wording (root `.claude-plugin/plugin.json` + `name` match → source root = working tree); cache Glob retained only in explicit fallback branches; consumers structurally unaffected.
- **/g-review Step 1 routine drift check added** — visible in the review record, never gates the verdict (four explicit never-gates statements).
- **Test surface grown 210 → 245 attested**: tests/test-g-doctor-drift.sh 3→12 (g-rules path + three-class classifier fixtures); NEW tests/test-class-split-invariant.sh 26 tests (six non-gating hooks exit 0 under representative + garbage payloads; gating pair structurally excluded per ADR-008 clause 6).
- **Durable pre-install snapshot** created at `D:\SW_Projects\G-Forge-snapshots\claude-install-snapshot-2026-07-21\` (34 `.claude/` + 14 git-hooks files + CLAUDE.md, byte-verified, restore command recorded) — supersedes the 2026-07-19 volatile temp snapshot (not deleted).
- **CLAUDE.md Quick Commands currency** synced to the attested per-suite table (new suite line, drift line updated, total 245).

## Decisions made
- **Invariant-suite failure adjudicated as test-artifact bug, not hook bug** (HQ empirical probe): two parallel attestation runners returned contradictory results (2/26 vs 26/26) on the same file; HQ reproduced both invocation forms — lazy `dirname "$0"` hook-path resolution after fixture `cd` broke relative invocation while absolute invocation masked it. Fixed attempt-2 (absolute resolution once at script top), re-attested green under the canonical invocation; retry logged to `.claude/escalation-log`.
- **Task 10's first DONE retracted on evidence** — its 26/26 was an invocation artifact; the contradictory-runner reconciliation (221 passed + 24 failed = 245 = 219 pre-existing + 26 new) was the tell.

## Patterns
### Worked well
- The Wave 2 attestation gate caught a real bug on the first-ever execution of a test-writer-authored suite (finding #20 discipline working as designed: DONE ≠ verified until a runner attests).
- Contradictory parallel attestations were resolved by HQ empirical probe before any redeploy — no thrash, one targeted attempt-2 fix.
- Wave-close integrity via full `git status` diff: tracked delta exactly the six scoped files at both checks; no doc-writer children fired (the Wave 1 hard rule held); no overreach for the second consecutive slice.
- Planned pass split absorbed the budget scenario exactly as the forecast recommended — the Wave 3→4 handoff was scheduled, not forced.
### Avoid / do differently
- Test suites must resolve script/repo paths to absolute ONCE at top, before any fixture `cd` — invocation-form sensitivity (relative vs absolute `$0`) produced a green run on one form and 24 failures on the other. W1.6 item queued: structural sweep of the other 10 suites for the same lazy-`dirname` class.
- Attestation prompts should pin the exact invocation form (repo-root relative) — the two Wave 2 runners diverged precisely because the form was unpinned.
- HQ arithmetic on expected totals is not evidence — the 247 estimate double-counted; only the runner's attested table is authoritative for counts.

## Cold-start context
**Branch:** main
**Active milestone:** M-audit-2026-07 — Forge Integrity (🔄; v2.3.0 at W1 close)
**Next up:** /g-execute W1.5g Wave 4 in a FRESH session — Pass 2 (Waves 4–5: LIVE non-gating install via the new self-host mechanism + extended-Check-16 verify, HQ-supervised) → /g-review (Pass 1+2 code) + commit + handoff. Pass 3 (Waves 6–7 Spike S1) deferrable.
**Key files touched:** SKILL.md (g-doctor, g-update, g-init, g-review), test-g-doctor-drift.sh, test-class-split-invariant.sh (new), CLAUDE.md (local), w1-5g plan/forecast, ROADMAP.md
**Carry-over context:** main @ `7479882`, 10 commits ahead of origin (unpushed), zero commits this pass. Rollback baseline: `D:\SW_Projects\G-Forge-snapshots\claude-install-snapshot-2026-07-21\`. Do NOT run stock /g-update — Wave 4 IS the install via the new mechanism; gating pair (check-commit.sh + native pre-commit) explicitly NOT installed until W1.7. ADR-007 verify-before-W2 still stands. stash@{0} superseded, droppable. Installed hooks still v2.2.1-era (journal logs agents "unknown" — #22 fix in source, goes live with Wave 4).

## Journal basis
25 events today (22 agent · 3 session) — all agent events "unknown" (installed observer predates the #22 fix; attribution from HQ context + agent-output records). Forecast w1-5g Outcome table reconciled Pass-1-partial: scenarios 1/2/4/5 did not happen, 3 unverified pending Pass 2.
