# Retro: adr-009-capture — 2026-07-24

## What was done
- Captured **ADR-009 — Split update integrity into detect / diagnose / fix contracts** (`g-docs/decisions/009-update-integrity-detect-diagnose-fix-split.md`, Accepted): the post-hoc record of M46's shipped decision. Mode-(a) pre-deliberated capture — raw inputs pulled from `g-docs/milestones/M46-update-integrity.md` + M46 retro instead of an interview; only the alternatives field needed gap-fill.
- Off-context deliberation subagent (single-use, per §C) stress-tested the draft and surfaced **two alternatives the developer had not named** (hook-level hard gate on the write; version pinning in the installed project) — both rejected with recorded deciding factors. Two of six WEAKNESSES findings (semver-vs-drift-check combined guarantee undocumented; no telemetry on preflight stops) folded into the ADR's Risks at developer approval.
- Reversibility check: **two-way door**; inline premortem recorded (most likely failure: Step-0 prose guard ignored by a future skill revision → stale realign recurs; earliest warning: consumer "weird update" report or post-update drift flag; escalation path: mechanical gate per rejected alternative 5).
- Verified **v2.4.0 fully released**: both manifests 2.4.0, CHANGELOG `[2.4.0] — 2026-07-23` cut, origin/main in sync at `51aa7bb`.
- Inventoried un-intaken/un-roadmapped backlog for the developer (3 intake-queue items, 3 parked M36-salience inputs, 6 riders, 1 untriaged pattern proposal).
- Session close-out: this retro + wholesale handoff rewrite + gated doc commit.

## Decisions made
- **Intakes bundle with the roadmap re-sequence at one gate** (developer, this session): `/g-intake`'s placement proposals would be immediately reshuffled by the three-horizon re-sequence, so all three intakes + §G patterns triage + `/g-roadmap` run together in the fresh session — one discussion, one write. Evidence: handoff Next-up rewritten to that order this pass.
- **ADR-009 folded weaknesses 2+6 into Risks** rather than leaving them decision-support-only (developer approval on recommendation).
- **Retro deferred to session end** covering ADR capture + close-out together rather than firing `/g-adr` Step 9's immediate retro — one retro, not two, for a session whose deliberation was already offloaded.

## Patterns
### Worked well
- Mode-(a) capture from milestone records: six of seven ADR fields mapped straight from `M46-update-integrity.md` + retro with zero interview questions; only alternatives needed a single gap-fill confirmation.
- Off-context deliberation earned its dispatch — 2 genuinely new alternatives and 6 weaknesses HQ's window never saw the reasoning for.
### Avoid / do differently
- The observer journal keys files by UTC date: this session (opened 22:53Z 2026-07-23) wrote all its events into the *previous* day's file, and `/g-retro`'s "read today's file" step found nothing dated today. Read session-open (`source: clear/startup`) boundaries, not calendar files, when scoping a session's events.

## Cold-start context
**Branch:** main
**Active milestone:** — (M46 ✅ shipped v2.4.0; next arc milestone M41 pending re-sequence)
**Next up:** FRESH SESSION `/g-resume` → verify ADR-009 → single gate: 3 intakes + §G patterns triage + `/g-roadmap` three-horizon re-sequence.
**Key files touched:** 009-update-integrity-detect-diagnose-fix-split.md, ROADMAP.md, reentry.md, 2026-07-24-adr-009-capture.md
**Carry-over context:** main @ v2.4.0 released and origin-synced; ADR-009 + handoff + retro ride this session's gated doc commit; g-wiki M46 refresh deferred in todo.md.

## Journal basis
1 session · 2 agent (deliberation subagent start/stop) + internal stops, all in the 2026-07-23 file's tail (UTC date rollover — see Patterns). No commit/test/push events yet at synthesis time; the close-out commit lands after this retro.
