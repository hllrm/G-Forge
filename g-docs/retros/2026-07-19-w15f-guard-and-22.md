# Retro: w15f-guard-and-22 — 2026-07-19

## What was done
- **W1.5f shipped (`e38f474`).** Full cycle in one session: `/g-patterns` (systemic-health mining) → apply pass → `/g-plan` W1.5f → `/g-forecast` (65% Elevated) → `/g-execute` (4 waves) → `/g-review` MERGE READY → `/g-doc-review` DOCS HOLD→READY → gated commit, both sentinels auto-consumed. 18 files, +594/−125.
- **Canonical worktree guard.** New shared `gf_guard_claude_dir()` in `hooks/lib/worktree-resolve.sh` (three-case non-gating contract) replaces the three divergent guard idioms across the six W1.3 hooks — each now carries the identical `GF_CLAUDE_DIR=$(gf_guard_claude_dir) || exit 0`. Conformance-invariant-pinned in `tests/test-worktree-resolve.sh` (25→42). The gating pair (`check-commit.sh`, `pre-commit`) deliberately excluded — fail-toward-deny must never silently exit 0; that exclusion is documented in the lib header and enforced by the test's scan-set.
- **Finding #22 fixed (pulled forward from W2 on developer order).** `agent-lifecycle.sh` had been probing four payload keys that don't exist (`agent_name/name/subagent_name/type`), journaling every agent as `unknown`. A TEMP probe in the *installed* hook captured real SubagentStart/Stop payloads from this session's own Wave-1 dispatch → ground truth: the field is `agent_type`. Extraction rewritten to target it, correlate via short `agent_id`, parse the `RESULT:` token from `last_assistant_message` on stops, and label empty-type internal agents `internal` — `unknown` now reserved for genuinely absent payloads. Real-payload fixtures in `tests/test-observe.sh` (16→22). Suite total 210.
- **`/g-patterns` apply pass (shipped-source, releasable lens).** Doc-writer overreach containment (scope-restricted child dispatch) applied to `templates/stack-implementer.md` + `agents/feature-implementer.md`; new "out-of-scope edit recovery = full-file diff, never spot-revert" rule in `rules/g-rules/C-agent-discipline.md`. New finding #27 recorded (g-doctor drift detection blind to installed rules/agents copies — silent fail-open, demonstrated live on this machine's own zero-g-rules install).

## Decisions made
- **#22 pulled forward into W1.5f** (developer, at the plan gate) — a consumer field bug (G-Tools BUG-3) degrading every consumer's learning loop; the releasable lens strengthened the case over leaving it in W2. Evidence: plan file #22 fold-in note, `e38f474`.
- **Design point (b) — shared lib function, not inline duplication** (decomposer recommendation, accepted) — the activation-only hooks can't host the redirect-value idiom inline, so only a shared function yields one literal canonical structure; no 13th install file. Evidence: task1-guard-function.md decision-matrix audit.
- **Gating pair stays raw-resolver** (HQ adjudication mid-Wave-3) — the 41/42 "failure" forced the explicit doctrine that the canonical guard is the NON-gating guard; check-commit/pre-commit fail-toward-deny by contract. Now a documented test exclusion, not an oversight.
- **Two test assertions adjudicated wrong-not-code** (HQ, Wave 3) — empty-stdin keeps `unknown` (reserved label); RESULT extraction is token-only by design. Same header-wrong-not-body-wrong pattern as W1.5b's rider 7a.

## Patterns
### Worked well
- **Probe-first discovery beat assumption.** #22 had been "fix the extraction" on the ledger for 6 days; the actual bug was unknowable without a real payload. Instrumenting the installed hook and triggering it with the session's own Wave-1 dispatch produced ground truth in one cycle — the system found a factual error about itself by looking, not remembering. Strongest instance yet of the ADR-004/verify-don't-trust ethos applied to a data-quality bug.
- **The no-child-dispatch hard line held again** (2nd enforced slice). Forecast scenario 1 (overreach) did not fire; wave-close integrity check showed only scoped files changed across all three Wave-2 agents. The `/g-patterns` rule edit applied earlier this same session (scope-restricted dispatch) is now doctrine backing the prompt-level line.
- **Both gates caught real currency gaps and cleared same-pass.** Doc gate HOLD on a stale header clause (the function's own "currently re-implements ad hoc" comment, falsified the moment Wave 2 landed) — exactly the class the gate exists for; fixed + re-verified in one turn.
- **Forecast discriminated correctly again.** 65% Elevated for a six-hook consolidation sat between W1.5c's 55 (one fixture) and W1.5d's 80 (the gate's parser) — the number tracked change-risk, not project size, per the standing calibration convention.

### Avoid / do differently
- **Session-limit kills now hit implementers, not just the runner** (3rd occurrence overall, 1st on a wave implementer). Task 3 was interrupted mid-verification with only narration emitted — resumed cleanly via SendMessage (context intact, no redeploy). Long single-dispatch tasks are the risk; consider tighter task-slicing or earlier dispatch when budget is deep. Distinct from a FAILED — an interrupted agent is resumable and must not be discarded.
- **#22's fix does not fix THIS machine's journal yet.** All 106 agent events today still logged `unknown` — because the fix is in *source*, but the *installed* `.claude/hooks/agent-lifecycle.sh` won't update until W1.7's local `/g-update`. The fix shipped to consumers before it fixed our own eyes; this retro's attribution is still HQ-context-and-git, not the journal. Verify the journal actually attributes at W1.7.
- **Budget ran hot** — the #22 fold-in pushed est. ~40 exchanges over a ~29 budget; the slice completed but on fumes. The forecast flagged this (scenario 2) and it held, but W1.6 (oversized) should start fresh, not continue here.

## Cold-start context
**Branch:** main
**Active milestone:** M-audit-2026-07 — Forge Integrity (🔄 in progress; ships v2.3.0 at W1 close)
**Next up:** `/g-plan` W1.6 (⚠ oversized, expect split) — tests for W1.3 + W1.4; carries W1.5f's 3 new minors (node-tier null→"null"; retired-token scan file-list vs dir; observe quote-safety line-2 gap). Then W1.7 (live verify, FIRST real stamped-sentinel + native pre-commit run, local /g-update — which finally installs the #22 fix here — v2.3.0 release pass incl. first README status strip + CHANGELOG cut).
**Key files touched:** hooks/lib/worktree-resolve.sh, six hooks (post-commit-cleanup/workflow-checkpoint/observe/agent-lifecycle/pre-compact/session-start), tests/test-worktree-resolve.sh, tests/test-observe.sh, templates/stack-implementer.md, agents/feature-implementer.md, rules/g-rules/C-agent-discipline.md, CHANGELOG.md, ROADMAP.md, M-audit-2026-07.md, w1-5f forecast, this retro.
**Carry-over context:** main @ v2.2.1, 9 commits ahead of origin (unpushed — developer's call). W1.5a–f all shipped. ADR-006 implementation post-M-audit; ADR-007 implementation = W2 (verify ADR-007 fresh-window before W2). Finding #27 (g-doctor rules/agents drift) → W1.6/W2. stash@{0} superseded, droppable. Installed-copy lag is now the dominant open risk (journal still blind, g-rules sections absent locally) — W1.7's /g-update is load-bearing.

## Journal basis
136 events today: 106 agent (ALL `unknown` — finding #22, fix shipped-to-source-not-yet-installed-here) · 22 session (multi-fire anomaly still visible — ruled registration-side, W1.7 check) · 4 commit. Attribution drawn from HQ context + git, not the journal — the last retro to have to say this, if W1.7's local install lands the #22 fix.

## Forecast outcome (w1-5f)
| Scenario | Predicted | Happened? | Notes |
|----------|-----------|-----------|-------|
| 1 behavior drift in consolidation | yes | partial | Two test-vs-contract adjudications (gating-pair scan, two assertions), absorbed in-wave; no actual hook behavior drift — decision-matrix audit + review spot-check confirmed behavior-identity |
| 2 budget overrun → handoff | yes | partial | Ran ~40 vs ~29 budget; completed on fumes, no mid-plan handoff needed, but W1.6 must start fresh |
| 3 parallel-wave overreach | yes | no | No-child-dispatch line held; wave-close integrity clean across 3 Wave-2 agents |
| 4 attestation dispatch killed | yes | happened (variant) | Session-limit kill hit a Wave-2 IMPLEMENTER (Task 3), not the attestation runner; resumed intact via SendMessage |
| 5 #21-class gate false-positive | yes | no | No gate false-fire this session; the gated commit passed clean |
