# Retro: M46-update-integrity — 2026-07-23

## What was done
- **M46 planned and approved** — 19 tasks / 6 waves, pre-split into 3 passes at wave boundaries (Pass 1 = Waves 1-2, Pass 2 = Waves 3-4, Pass 3 = Waves 5-6), fresh session per pass (commit `957a6ca`).
- **Pass 1 (Waves 1-2) — stop the bleeding.** Authored the shared `hooks/lib/semver-compare.sh` (`gf_semver_compare`, -1/0/1 contract) plus its test suite (attested 26/26); made `workflow-checkpoint.sh`'s update line direction-aware (no more backwards "update available" advisories); added `/g-update`'s staleness preflight (stale cache ⇒ stop, write nothing). Pass-after checkpoint suite 80/80 (HQ run). Committed `4dde7e5`.
- **Pass 2 (Waves 3-4) — the contract split.** Attested the staleness-preflight fixture after a real BLOCKED-and-fix cycle (fixture call site missing a `detail` arg, `set -u` crash at line 54 → surgical fix → re-attested 13/13); `/g-update` shed its diagnostic overlap with `/g-doctor` (grep-provable single owner per check); eager-mirrored the installed hook copies with byte-hash verification on both files; docs swept across skill descriptions, README, G-RULES §B, and the CLAUDE.md test table (77→80 suite rows, total 493→522, HQ-sum rule). Committed `886aad5`.
- **Pass 3 (Waves 5-6) — close and release.** `/g-blast-radius` on the version-triple primitive (new finding: `project-manager.md:21` legacy direction-blind version check, deferred to backlog) + CHANGELOG `[Unreleased]` entry; dual version bump to v2.4.0. Work commit `e3d9d71` gated: 522/522 attested + fixture 13/13; code-lead HOLD (task-17 doc-writer duplicated a truncated CHANGELOG bullet) → fix → MERGE READY on scoped re-review; doc-review HOLD (stale 22-point/6-advisory counts at `README.md:549` and `g-wiki/usage.md:222`, task-15/wiki sweep misses) → fix → DOCS READY. Release commit `ee4cc6b`: code-lead MERGE READY (0 findings); doc-review HOLD (stale `## Active Session` handoff contradicting M46 ✅) → handoff rewritten wholesale → DOCS READY r2. Pushed; M46 shipped as v2.4.0.
- **Live handoff-banner bug found and fixed during the release doc-gate.** Root cause: the `## Active Session` block had accumulated `─── prior pass ───` residue across passes instead of being replaced wholesale, and the current pass had dropped the labeled `Active context:` line `workflow-checkpoint.sh` greps for — together these served a week-stale checkpoint banner. HQ rewrote the block wholesale (prior-pass residue purged). A second, related bug surfaced live in the same pass: the hook's greedy label-strip matches the LAST occurrence of the label literal, and the rewritten block's own format note mentioned the label text twice — deduplicated and re-verified by the same reviewer as a post-verdict follow-up (not a redeploy).

## Decisions made
- Adopted a single shared comparison implementation (`hooks/lib/semver-compare.sh`) consumed by `workflow-checkpoint.sh`, `/g-doctor`, and `/g-update`, replacing three independent hand-rolled compares — the milestone's own origin bug (evidence: plan header `g-docs/plans/m46-update-integrity.md:9`, task 4 done condition "no second hand-rolled compare," extended repo-wide at Wave 3 close per the plan Progress table).
- Fixed the detect/diagnose/fix ownership split as a hard contract: `workflow-checkpoint.sh` detects, `/g-doctor` diagnoses (read-only), `/g-update` is the sole writer — inferred from the milestone goal statement (`g-docs/milestones/M46-update-integrity.md:6`) and task 12/13 done conditions ("grep-provable single owner per check").
- The pre-approved 3-pass split was honored exactly as scheduled rather than renegotiated mid-session: three session-open events (16:23:44, 17:25:43, 19:35:48 UTC) each align 1:1 with a pass-boundary commit (`4dde7e5`, `886aad5`, `e3d9d71`/`ee4cc6b`).

## Patterns
### Worked well
- Fresh session per pass held exactly as planned — no unplanned mid-wave handoff was forced (forecast scenario 5 did not materialize as stated), even though code-lead's first release-adjacent review ran ~55 minutes legitimately.
- Attestation discipline held throughout: every gate cites real per-suite runner output (26/26, 80/80, 13/13, 522/522) with HQ-summed totals, never a re-derived or confabulated number (forecast scenario 1 mitigated).
- The Wave-3-close repo-wide compare-idiom grep (scheduled specifically to catch the milestone's own origin-bug class recurring) came back clean with a single documented WHY-commented exception (forecast scenario 2 mitigated).
- An interrupted doc-writer agent on the Wave 4 README task was resumed to completion per the §C carve-out rather than redeployed — record verified by HQ, no context poisoning.
- Both HOLD verdicts (work-commit code-lead, work-commit doc-review, release doc-review) were closed with a scoped re-review of only the fixed finding, not a full re-review cycle.

### Avoid / do differently
- Two `code-lead` dispatches and one `doc-reviewer` dispatch lacked Write in their dispatch context and returned records inline per §C instead of writing files directly; HQ had to transcribe them with provenance notes, and one record name-collided with v2.3.0's same-day release record, requiring a `-v240` suffix to persist without overwrite. Date-keyed record filenames collide across same-day releases.
- Doc-gate HOLD (r1) on the work commit and the release-pass doc-gate HOLD both trace to the same root class: stale documentation counts/strings not caught until the gate, not earlier in the sweep — this is a recurrence of a class already named in `g-docs/retros/2026-05-19-m10-m14-pre-shipping-sweep.md` ("Documentation currency check at code-lead time, not earlier" / "three of five milestones had stale README counts"). This milestone hit it twice in one pass (`README.md:549` and `g-wiki/usage.md:222` both missed the same count pair; the release handoff staleness is a second instance of the same underlying miss-until-gate pattern) — a repo-wide count/string grep run as part of the doc sweep itself, before the gate, would catch both sites in one pass instead of relying on the reviewer to find them.
- The handoff-rewrite rule ("replace, never append") was violated by residue accumulating across passes, and separately the hook's greedy label-strip assumes the label literal appears exactly once in the block — both are now format constraints worth stating explicitly wherever the handoff format is documented, not just discovered live at a release gate.
- Record-dir reuse leftovers (stray files dated 2026-07-10) resurfaced in the Wave 4 and Wave 5 agent-output directories for a second time this milestone — pre-existing, not this wave's writes, but noted twice now without a cleanup action taken.
- Task 10 (fixture attestation) hit a real BLOCKED on first attempt from a fixture call site missing a `detail` argument (`set -u` crash at line 54) — a plain authoring bug, not the MSYS timestamp-granularity/platform-timing failure mode the forecast anticipated for this task (scenario 4).

## Cold-start context
**Branch:** main
**Active milestone:** M46 — Update Integrity — ✅ Complete, shipped v2.4.0 (2026-07-23)
**Next up:** `/g-retro` (M46) + milestone close swarm (`/g-patterns`, `/g-telemetry`, `/g-align`) · then `/g-plan` M41 — Release Machinery + README Currency (v2.5.0) · fold developer+friend milestone-board rankings into arc sequencing when provided (board artifact add745a4).
**Key files touched:** `hooks/lib/semver-compare.sh`, `hooks/workflow-checkpoint.sh`, `tests/test-semver-compare.sh`, `tests/test-workflow-checkpoint.sh`, `skills/g-update/SKILL.md`, `skills/g-doctor/SKILL.md`, `g-dev/fixtures/g-update-staleness-preflight.sh`, `README.md`, `rules/g-rules/B-workflow.md`, `CLAUDE.md`, `CHANGELOG.md`, `plugin.json`, `marketplace.json`, `g-docs/blast-radius/M46-wave1-version-triple.md`, `g-docs/ROADMAP.md`, `g-docs/milestones/M46-update-integrity.md`
**Carry-over context:** main @ v2.4.0 (M46 Update Integrity ✅ — detect/diagnose/fix split live: checkpoint direction-aware nudge, `/g-doctor` Check 23, `/g-update` Step-0 preflight, shared semver lib sole ordering contract). Arc next: M41 (v2.5.0) → M45 (v2.6.0) → M42 (v2.7.0) → M29 (v2.8.0) → M35 → M36/M37 → M33 → M38–M40/M43 → M44 G-Proof 1.0 capstone. Backlog riders: NEW `project-manager.md:21` legacy direction-blind version check (blast-radius finding — retire in favor of checkpoint detect or route through lib) · NEW `/g-doctor` Check-16 lib enumeration stale (covers 4 of 6 libs — `stdin-read.sh` + `semver-compare.sh` missing; README "4 lib scripts" mirrors the skill, fix both together) · `command -v` hardening · fixture sleep-leak · per-session counter accumulation · ADR-006 implementation.

## Journal basis
174 agent · 5 commit · 4 push · 3 session (M46 portion of `.claude/journal/2026-07-23.jsonl`, plan commit through release push). The same day's journal file also holds 150 agent · 4 commit · 5 push · 2 session events preceding M46's plan commit, covering the prior M-audit-close / v2.3.0 release finalization — not part of this milestone and excluded from the counts above.
