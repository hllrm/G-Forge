---
description: Run the full review pipeline on the current branch diff. Issues MERGE READY or HOLD.
---

Use Glob to find `skills/g-review/SKILL.md` inside `~/.claude/plugins/cache/g-forge/g-forge/` and read it, then follow its instructions exactly.
