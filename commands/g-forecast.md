---
description: Run scope-realism analysis and premortem on a plan. Outputs a complexity score (0–10), a miss-risk percentage, and a ranked list of failure scenarios seeded by /g-patterns history. Advisory — never blocks plan approval.
---

Use Glob to find `skills/g-forecast/SKILL.md` inside `~/.claude/plugins/cache/g-forge/g-forge/` and read it, then follow its instructions exactly. The user's argument (if any) is: $ARGUMENTS
