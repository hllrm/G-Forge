---
description: Build and maintain a human-facing project wiki in g-wiki/ — narrative docs of what the project is, how it's architected, and how to use it. Run anytime; queued at the end of every milestone. Distinct from /g-docs (code-level doc hygiene).
argument-hint: "[area or topic | full]"
---

Use Glob to find `skills/g-wiki/SKILL.md` inside `~/.claude/plugins/cache/g-forge/g-forge/` and read it, then follow its instructions exactly. The user's argument (if any) is: $ARGUMENTS
