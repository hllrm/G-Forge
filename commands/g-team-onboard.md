---
description: Onboard G-Team onto an existing codebase. Reads the repo, interviews about what's next, and produces project_brief.md. Run before init and specialize on existing projects.
---

Invoke the `g-team:g-team-onboard` skill.

If the skill content does not load (you only see "Launching skill" with no further instructions), use Glob to find `skills/g-team-onboard/SKILL.md` inside `~/.claude/plugins/cache/g-team/g-team/` and read it directly, then follow its instructions exactly.
