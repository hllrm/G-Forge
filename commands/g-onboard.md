---
description: Onboard G-Forge onto an existing codebase. Reads the repo, interviews about what's next, and produces g-docs/project_brief.md. Run before init and specialize on existing projects.
---

Use Glob to find `skills/g-onboard/SKILL.md` inside `~/.claude/plugins/cache/g-forge/g-forge/` and read it, then follow its instructions exactly.
