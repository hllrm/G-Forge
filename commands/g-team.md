---
description: Deprecated alias for /g-forge. Use /g-forge instead. Routes the same subcommands (help, status, doctor, init, plan, execute, review, …).
argument-hint: <help|status|doctor|init|kickoff|onboard|brief|roadmap|plan|execute|review|afk|specialize|update|skill-design|skill-validate|patterns|forecast|telemetry|blast-radius|identity|tier|voice> [args]
---

⚠️ `/g-team` is a **deprecated alias** for `/g-forge` and will be removed in a future major version. Prefer `/g-forge <subcommand>`.

To handle this invocation: use Glob to find `commands/g-forge.md` inside `~/.claude/plugins/cache/g-forge/g-forge/`, read it, and follow its routing for the subcommand in $ARGUMENTS exactly as written.
