---
description: Proactive feature-drop triage — classify a dropped feature against the brief (on-brief / scope-creep / out-of-scope), propose placement, version impact, and a risk hint, then ask before writing anything. The lightweight front-end to /g-roadmap for single ideas.
argument-hint: [feature idea]
---

Use Glob to find `skills/g-intake/SKILL.md` inside `~/.claude/plugins/cache/g-forge/g-forge/` and read it, then follow its instructions exactly. The user's argument (if any) is: $ARGUMENTS
