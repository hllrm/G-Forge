---
description: Run the documentation review gate. Checks documentation currency and coverage against the current branch diff, issuing a DOC PASS or DOC HOLD verdict.
argument-hint: [path/to/scope | all]
---

Use Glob to find `skills/g-doc-review/SKILL.md` inside `~/.claude/plugins/cache/g-forge/g-forge/` and read it, then follow its instructions exactly. The user's argument (if any) is: $ARGUMENTS
