---
description: Analyse blast radius of a planned change. Inputs a file path, plan slug, or feature label. Outputs forward + reverse dependency graph, per-file volatility, and an aggregate rating (Narrow / Moderate / Wide). Persists report for /g-forecast integration.
---

Use Glob to find `skills/g-blast-radius/SKILL.md` inside `~/.claude/plugins/cache/g-forge/g-forge/` and read it, then follow its instructions exactly. The user's argument (if any) is: $ARGUMENTS
