---
description: Set how G-Forge communicates with you. No argument runs a short 2-question intake and sets the right style automatically — no tier names to memorise. With `dev`, `mid`, or `eli5`: applies that profile directly. Profile changes rendering across every skill — same facts, same verdicts.
---

Use Glob to find `skills/g-voice/SKILL.md` inside `~/.claude/plugins/cache/g-team/g-team/` and read it, then follow its instructions exactly. The user's argument (if any) is: $ARGUMENTS
