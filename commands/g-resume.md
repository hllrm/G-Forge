---
description: Re-hydrate a fresh session with the right slice of the durable record — selectively retrieves the relevant retro, ADRs, journal, and handoff keyed by the current branch/milestone/first-task and assembles a focused re-entry briefing. The read-side counterpart to /g-retro. Loads distilled context into a clean window.
argument-hint: [optional focus — e.g. an ADR number or milestone]
---

Use Glob to find `skills/g-resume/SKILL.md` inside `~/.claude/plugins/cache/g-forge/g-forge/` and read it, then follow its instructions exactly. The user's argument (if any) is: $ARGUMENTS
