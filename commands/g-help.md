---
description: Context-aware help. With no argument, reads project state and tells you where you are and what to do next, plus a map of every archive. With a topic/question (`/g-help <topic>`), answers it and points you at the right command or archive.
argument-hint: "[topic or question]"
---

Use Glob to find `skills/g-help/SKILL.md` inside `~/.claude/plugins/cache/g-forge/g-forge/` and read it, then follow its instructions exactly. The developer's topic/question, if any, is: $ARGUMENTS
