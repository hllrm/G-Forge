---
description: Health check for g-team project setup. Verifies hooks, settings, CLAUDE.md rules block, commit sentinel, plan files, and milestone alignment.
---

Use Glob to find `skills/g-doctor/SKILL.md` inside `~/.claude/plugins/cache/g-forge/g-forge/` and read it, then follow its instructions exactly.
