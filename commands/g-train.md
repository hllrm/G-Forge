---
description: Training mode — learn software development by building a real project. Runs the full G-Forge workflow with a teaching layer at every stage and tasks alongside the agent swarms calibrated to your level. No project idea? G-Forge generates one.
---

Use Glob to find `skills/g-train/SKILL.md` inside `~/.claude/plugins/cache/g-forge/g-forge/` and read it, then follow its instructions exactly. The user's argument (if any) is: $ARGUMENTS
