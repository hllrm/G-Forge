---
description: Documentation audit and generation. Scans for missing or stale code docs, README gaps, undocumented env vars, CHANGELOG gaps, and decisions without ADRs. Targeted scope fixes gaps via doc-writer. Whole-codebase produces a prioritised debt report and optional roadmap entry.
argument-hint: [path/to/scope | all]
---

Use Glob to find `skills/g-docs/SKILL.md` inside `~/.claude/plugins/cache/g-forge/g-forge/` and read it, then follow its instructions exactly. The user's argument (if any) is: $ARGUMENTS
