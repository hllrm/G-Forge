---
description: Design a new G-Forge skill from scratch — frontmatter, announce line, steps, rules section, companion command file, and router registration.
---

Use Glob to find `skills/g-skill-design/SKILL.md` inside `~/.claude/plugins/cache/g-forge/g-forge/` and read it, then follow its instructions exactly.
